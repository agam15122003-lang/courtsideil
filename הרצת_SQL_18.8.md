# הרצת ה-SQL — 18.8.2026

**שני קבצים להריץ, בסדר הזה.** אפשר להריץ אותם בזה אחר זה באותו יום.

מריצים ב-**Supabase → SQL Editor → New query**, מדביקים את כל הקובץ, Run.
בטוח להרצה חוזרת (אם תריץ פעמיים לא יקרה כלום רע).
בסוף תסתכל שאין **ERROR אדום**. שורות `NOTICE` ירוקות — תקינות ומכוונות.

---

## מה להריץ

| # | קובץ | מה הוא נותן | סטטוס |
|---|---|---|---|
| 38 | `supabase_notebook_18_8.sql` | כל מה שהמחברת החדשה צריכה: הטקסט, כתב היד, המגרשים, הקבוצה, התאריך, המשך, «טיוטה», סיבת היעדרות, ומה השחקן רואה מתוכנית ששלחת | ✅ רץ 18.8 |
| 39 | `supabase_dossier_18_8.sql` | «תיק שחקן»: זהות אחת לכל שחקן שעוברת בין שנים, קטלוג קטגוריות (16 דירוגים + 4 מדידות), הערכים עם תאריך, רקע ושיחות, הרשאות, והמבנה במועדון | ⏳ **מחכה להרצה** |

שני הקבצים יושבים בתיקיית הפרויקט, לצד כל שאר קבצי ה-SQL.

---

## למה זה חוסם

בדקתי במסד שלך היום — העמודות החדשות עוד לא קיימות.
**עד שהקובץ ירוץ, שמירה במחברת תשמור רק את שם התוכנית** ותקפיץ הודעה אדומה:
הטקסט, כתב היד, המגרשים והנוכחות לא יישמרו. לכן לא העליתי את השינוי לאוויר.

**הסדר: קודם ה-SQL, אחר כך אני מעלה את הקוד.** לא הפוך.

---

## בדיקה אחרי קובץ 38 (המחברת)

שלוש שורות. הראשונה היא הבדיקה האמיתית — השתיים האחרות לשקט נפשי.

```sql
-- 1. העמודות החדשות קיימות? (צריך לחזור 4 שורות)
select column_name from information_schema.columns
 where table_name = 'training_plans'
   and column_name in ('body', 'ink', 'courts', 'is_draft');

-- 2. הקובץ נרשם ביומן ההרצות? (צריך להופיע למעלה)
select filename, ran_at from public.schema_migrations
 order by ran_at desc limit 5;

-- 3. הפונקציה החדשה לא מדליפה כלום (צריך לחזור null)
select public.plan_for_player('00000000-0000-0000-0000-000000000000');
```

אם 1 חזרה עם 4 שורות — סיימת. תגיד לי ואני מעלה לאוויר.
אם חזרה שגיאה — תדביק לי אותה בדיוק כמו שהיא, לא צריך לתקן כלום בעצמך.

---

## בדיקה אחרי קובץ 39 (תיק שחקן)

```sql
-- 1. הקטלוג נטען? (צריך לחזור 20)
select count(*) from public.dossier_metrics where club is null;

-- 2. הטבלאות קיימות? (צריך לחזור 6 שורות)
select table_name from information_schema.tables
 where table_schema = 'public'
   and table_name in ('dossier_people','dossier_metrics','dossier_entries',
                      'dossier_notes','dossier_access','club_roles');

-- 3. ההרשאה עובדת ולא מדליפה (צריך לחזור false)
select public.dossier_can_see('00000000-0000-0000-0000-000000000000');
```

### ואחרי זה — מינוי מנהל מועדון (רק אתה יכול)

התפקיד הזה רואה את התיקים של כל המאמנים שצורפו למועדון, ולכן הוא נקבע
רק בידי אדמין המערכת — אתה. החלף את המייל ותריץ:

```sql
insert into public.club_roles (club, user_id, role, approved_by)
select p.club, p.id, 'club_manager', p.id
  from public.profiles p
 where p.id = (select id from auth.users where email = 'המייל-של-המנהל')
on conflict do nothing;
```

מנהל המועדון ממנה מנהל מקצועי ומצרף מאמנים (`role = 'coach'`) — הוא יכול
לעשות זאת מהאפליקציה ברגע שנבנה לו מסך; עד אז זה שורה במסד.
**מאמן שלא צורף — התיקים שלו פרטיים לחלוטין**, וזה בכוונה.

## מה זה משנה על המסך (אחרי שאעלה)

| מה | לפני | אחרי |
|---|---|---|
| בניית תוכנית | שחזור טיוטה אוטומטי + «מיקוד האימון» + חלקים ממוספרים | דף מחברת אחד: כותבים או כותבים ביד, בלי חלקים |
| שמירה | נשמר תמיד | «שמירת התוכנית» או «שמור כטיוטה». יציאה בלי לשמור — מזהיר |
| מגרשים | רק בתוך תרגיל | שלושה בצד שמאל של הדף, מציירים עליהם ישר |
| נוכחות | מסך נפרד | בתחתית הדף. כולם «מגיע», מסמן רק מי שלא + סיבה |
| קטגוריה בתרגיל | שש אפשרויות קבועות | מלל חופשי (שכבות הגיל נשארו) |
| שליחה לשחקנים | רק שם התוכנית | בוחר בכל שליחה: רשימת התרגילים או הדף כולו |

**תוכניות ישנות לא נעלמות:** תוכנית שנבנתה בחלקים תיפתח כטקסט על הדף, והתרגילים
שהיו בה נשארים מחוברים אליה.

### תיק שחקן (קובץ 39)

| מה | איפה |
|---|---|
| כל שחקן בסגל מקבל תיק אוטומטית בפתיחה הראשונה | «תיק שחקן» בסרגל, או הכפתור בכרטיס השחקן |
| 16 דירוגים 1–5 + 4 מדידות, עם תאריך | בתיק, או «סבב דירוג» לכל הקבוצה |
| גרף התקדמות | נפתח מהדירוג/מדידה השנייה ומעלה |
| רקע ושיחות | טקסט חופשי עם סוג ותאריך |
| מי רואה | אתה + המנהלים במועדון (אם צורפת) + מי שנתת לו גישה. **לא** השחקן ולא ההורים |

**חשוב:** שחקן שכבר קיים אצלך משנה קודמת יזוהה («זה אותו שחקן?») ואפשר
לחבר את התיקים — ואז הגרף מציג את כל השנים.

---

## אם תרצה לבטל

הקובץ רק **מוסיף** עמודות — הוא לא מוחק ולא משנה נתונים קיימים.
אם תחליט לחזור אחורה, לא נוגעים במסד: פשוט לא מעלים את הקוד לאוויר,
והאתר ממשיך לעבוד כמו היום. העמודות החדשות סתם יישבו שם ריקות.
