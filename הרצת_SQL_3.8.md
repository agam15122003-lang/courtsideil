# הרצת ה-SQL — מצב נכון ל-4.8.2026

זה המסמך שאומר לך איפה הדברים עומדים. קרא אותו מלמעלה למטה.

**בקצרה:** כל 14 הקבצים רצו בייצור. **אין SQL שממתין להרצה.** מה שנשאר הוא
רשימה קצרה של דברים שרק אתה יכול לעשות — כי הם בדשבורד ולא בקוד.

מריצים ב-**Supabase → SQL Editor → New query**, קובץ אחד בכל פעם.
כל קובץ בטוח להרצה חוזרת. אם משהו נכשל — תקן והרץ את אותו קובץ שוב.

---

## 1. מה כבר רץ ✅

| # | קובץ | מה הוא נתן |
|---|---|---|
| 1 | `supabase_rls_hardening_3_8.sql` | סגירת חמש הבעיות הקריטיות בהרשאות |
| 2 | `supabase_rls_gaps_3_8.sql` | חמש הרשאות פתוחות ששרדו, ובראשן אישור סרטוני קטינים |
| 3 | `supabase_parent_consent.sql` | מערכת הסכמת ההורים |
| 4 | `supabase_consent_manage_fix.sql` | קישור ניהול חוזר להורה + תיקון הסכמה בידי מנהל |
| 5 | `supabase_admin_requests.sql` | ערוץ פנייה למנהל + החלפת ההורה הרשום |
| 6 | `supabase_consent_enforcement.sql` | ההגבלות נאכפות בשרת, לא רק במסך |
| 7 | `supabase_hardening_medium_3_8.sql` | הקשחות בינוניות + אינדקסים |
| 8 | `supabase_private_media.sql` | תמונות וסרטונים נפתחים רק בקישור חתום |
| 9 | `supabase_age_crosscheck.sql` | השוואה בין הגיל שהשחקן הצהיר לגיל שהמאמן הזין |
| 10 | `supabase_my_data.sql` | "המידע שלי" — עיון והורדה של כל המידע |
| 11 | `supabase_audit_retention.sql` | יומן תיעוד שינויים + הגדרת תקופות השמירה |
| 12 | `supabase_no_player_avatars.sql` | לשחקנים אין תמונת פרופיל, והתמונות הישנות נמחקו |
| 13 | `supabase_retention_schedule.sql` | מתזמן את הניקוי, ומוסיף דוח "מתי זה רץ לאחרונה" |
| 14 | `supabase_account_deletion.sql` | מחיקת חשבון אמיתית + דוח "מה יימחק" לפני שמוחקים |

לבדיקה שהכול באמת שם:

```sql
select filename, ran_at from public.schema_migrations order by ran_at desc limit 20;
```

**אומת ב-4.8.2026 — כל 14 השורות חזרו.**

---

## 2. מה נשאר להריץ

**כלום.** כל הקבצים של הגל הזה רצו.

שני דברים שכדאי לזכור לפעם הבאה:

**`supabase_retention_schedule.sql` לא מוחק כלום.** הוא מתזמן בדיקה יומית
שסופרת כמה שורות היו נמחקות, כותבת את המספר, וממשיכה הלאה. המחיקה בפועל
מופעלת בנפרד, ורק אחרי שעורך דין מאשר את התקופות — ראו סעיף 5.

**כשקובץ SQL מתוקן, צריך להריץ אותו שוב** גם אם כבר רץ פעם אחת. הקבצים בטוחים
להרצה חוזרת. אם נוספו לתיקייה קבצי `supabase_*.sql` שאינם בטבלה של סעיף 1,
הרץ גם אותם, לפי סדר התאריך שלהם.

**אחרי כל קובץ תסתכל שאין ERROR אדום בסוף.** שורות `NOTICE` ירוקות הן תקינות
ומכוונות — הקבצים מדווחים מה הם עושים.

---

## 3. בדיקות אחרי ההרצה

**א. אין טבלה שכל משתמש קורא במלואה:**

```sql
select tablename, policyname from pg_policies
where schemaname = 'public' and qual = 'true';
```

חייבת לחזור שורה אחת בלבד — `consent_documents`. זו הטבלה עם נוסח ההסכמה,
וההורה חייב לקרוא אותה בלי חשבון. אין בה שום מידע אישי.
כל שורה אחרת שתופיע כאן היא בעיה שצריך לסגור.

**ב. הניקוי מתוזמן:** ✅ **אומת ב-4.8.2026 — מתוזמן ופעיל, סופר ולא מוחק.**

ב-SQL Editor השתמש בשתי אלה. הראשונה צריכה להחזיר שורה, והשנייה צריכה
להחזיר `active = true` על `25 3 * * *` (שעון UTC — 6:25 בבוקר בישראל בקיץ):

```sql
select extname, extversion from pg_extension where extname = 'pg_cron';
select jobname, schedule, active from cron.job where jobname = 'courtside-retention-purge';
```

אם השנייה חוזרת ריקה — הרץ שוב את `supabase_retention_schedule.sql`.

**מתוך האתר**, מחשבון מנהל מחובר, יש דוח מלא:

```sql
select public.admin_retention_status();
```

⚠ **היא נכשלת ב-SQL Editor** עם «למנהלים בלבד» — בעורך אינך מחובר כמשתמש,
ולכן היא לא מזהה אותך כמנהל. זו אינה תקלה. תסתכל על השדה `state`:

* `not_scheduled` — אין תזמון. משהו לא הופעל. ראו סעיף 4.
* `dry_only` — מתוזמן ורץ, סופר ולא מוחק. **זה המצב הנכון היום.**
* `running` — מתוזמן ומוחק בפועל.
* `stale` — היה אמור לרוץ ולא רץ שבוע. ללכת לבדוק.

**ג. בדיקה חיה באתר:** בית המאמן, הסגל, תרגילים, קהילה, הודעות, ומסך שחקן.

---

## 4. מה שרק אתה יכול לעשות

אלה דברים שאי אפשר לקבע בקוד. הם בדשבורד או בחשבון חיצוני.

**1. הגדרות הסיסמאות** — Authentication → Sign In / Providers → Password.
היום הדשבורד מאפשר 6 תווים. הקוד באפליקציה חוסם מתחת ל-8, אבל זו רק הודעה
יפה למשתמש; מי שפונה לשרת ישירות עוקף אותה. **ההגנה האמיתית היא ההגדרה
בדשבורד, לא הקוד.** קבע מינימום 8 תווים, והפעל בדיקת סיסמאות שדלפו.
אל תקבע יותר מ-8 בלי לעדכן קודם את הקוד — יופיעו הודעות שגיאה שסותרות את עצמן.

**2. הגנות נוספות באימות** — CAPTCHA בהרשמה ובהתחברות, הגבלת קצב על מיילים,
ואימות מייל בהרשמה. וכן `ban_duration`, כדי שחסימה תנתק את המשתמש מיד ולא רק
תמנע ממנו לכתוב.

**3. מפתח Google/YouTube.** היה בהיסטוריית הגיט. אם לא סובב — סובב עכשיו,
והגבל אותו ל-YouTube Data API v3, עם דומיין `courtsideil.vercel.app` ותקרה יומית.

**4. הפעלת pg_cron** — Database → Extensions → חפש `pg_cron` → Enable.
בלי זה אין תזמון לניקוי. אחרי ההפעלה הרץ שוב את
`supabase_retention_schedule.sql`, או הדבק את השורה שהוא הדפיס לך.

**5. אחסון התמונות.** ודא שה-bucket בשם `media` מוגדר Private:

```sql
select id, public from storage.buckets where id = 'media';
```

אם `public` הוא `true` — תמונות עדיין נגישות לכל מי שיש לו קישור. להיפוך:

```sql
update storage.buckets set public = false where id = 'media';
```

לפני ההיפוך תיכנס לאתר ותוודא שהתמונות עולות. אם משהו נשבר, מחזירים ל-`true`
באותה שנייה.

**6. גיבויים.** Database → Backups. ודא שהם פעילים, ושניסית פעם אחת לשחזר.

---

## 5. הדבר האחד שדורש עורך דין לפני שממשיכים

תקופות השמירה — כמה זמן נשמר כל סוג מידע לפני שנמחק — נקבעו על ידי צוות
הפיתוח. הן סבירות, אבל **אף אחד לא אישר אותן משפטית**.

לראות אותן:

```sql
select key, days, method, what from public.retention_policy order by key;
```

לראות כמה שורות היו נמחקות היום (לא מוחק כלום):

```sql
select * from public.admin_purge_report();
```

**אל תפעיל מחיקה בפועל לפני שכל אלה קרו:**

1. עורך דין אישר את התקופות.
2. יש גיבוי טרי.
3. הסתכלת על המספרים בדוח למעלה והם נראים לך הגיוניים.

ורק אז, שורה אחת:

```sql
update public.retention_schedule
   set armed = true,
       lawyer_ok_at = now(),
       lawyer_note = 'מי אישר ומה בדיוק אושר',
       updated_at = now()
 where id;
```

המסד לא ייתן לך להפעיל בלי למלא את השדה של האישור. זה מכוון.
לכיבוי בכל רגע — אותה שורה עם `armed = false`.

אחרי מחיקה אין חזרה.

---

## 6. איך המערכת עובדת היום — בקצרה

**המאמן הוא שומר הסף.** שחקן נכנס עם קוד קבוצה. קטין שההורה שלו טרם אישר
**לא נחסם** — הוא נכנס, עורך פרופיל ומצטרף לקבוצה. מה שחסום לו: תמונות,
קהילה, צ'אטים והודעות. המאמן רואה במסך אישור הבקשות אם ההורה אישר.

**הורה שביטל הסכמה** — החשבון מושעה. גם משם אפשר לפנות למנהל.

**לשחקנים אין תמונת פרופיל.** לא בטופס, ולא בשרת. מה שלא נאסף לא יכול לדלוף.

**יש יומן.** שינוי בשדה רגיש נרשם: מי שינה, מתי, על מי. התוכן עצמו לא נשמר
ביומן — רק העובדה שהשדה שונה. **היומן מתעד שינויים, לא צפיות.**

**יש "המידע שלי".** שחקן רואה במסך ההגדרות את כל מה שמוחזק עליו, ויכול להוריד
לקובץ. גם ההורה יכול, מתוך קישור הניהול שלו.

---

## 7. מה נשאר פתוח ביודעין

**אין אימות טכני שמי שמאשר הוא ההורה.** אין תשתית מייל, ולכן הקישור עובר דרך
הקטין. מה שכן נסגר: אי אפשר להסיט את הקישור למייל אחר, אי אפשר לחדש הסכמה
שההורה ביטל, ואי אפשר לדרוס סירוב. הפיצוי הוא המודל שבחרת — המאמן מכיר את
המשפחה. אכיפה מלאה דורשת שליחת מייל להורה, וזה מצריך ספק מייל.

**הצלבת הגיל אינה בדיקת זהות.** היא מסמנת מקרה שבו השחקן והמאמן אמרו שני
דברים שונים. ילד שאמר לשניהם את אותו דבר לא יתגלה.

**מחיקת חשבון היא ידנית.** בקשה נרשמת, ואתה מוחק בפועל ומסמן. ההתחייבות
במדיניות היא 30 יום.

**היומן לא מתעד צפיות**, רק שינויים.

**המחיקה האוטומטית עדיין לא פועלת** — היא רק סופרת, עד סעיף 5.

**וכל הנוסחים המשפטיים דורשים עורך דין לפני גבייה או השקה רחבה.**
