# הרצת ה-SQL — 3.8.2026

הקוד כבר עלה לאוויר. **חמש המיגרציות עדיין לא רצו** — עד שתריץ אותן, האפליקציה
ממשיכה לעבוד בדיוק כמו קודם (כל הקוד החדש נופל אחורה כשהעמודה או ה-RPC חסרים),
אבל החורים שהדוח מצא נשארים פתוחים.

מריצים ב-**Supabase → SQL Editor → New query**, קובץ אחד בכל פעם, לפי הסדר.

---

## לפני שמתחילים — שתי בדיקות

```sql
select count(*) filter (where role = 'player') as players,
       count(*) filter (where role = 'player' and birth_year is null) as no_birth_year
from public.profiles;
```

`no_birth_year` גדול מ-0? אלה שחקנים שיסומנו `pending_parent` ויידרשו אישור הורה.
זה המצב הנכון — רק תדע למי לצפות.

```sql
select id, public from storage.buckets where id = 'media';
```

`public = true` זה המצב היום. שלב 5 הופך אותו לפרטי — ורק בסוף.

---

## סדר ההרצה

| # | קובץ | מה קורה | חובה? |
|---|---|---|---|
| 1 | `supabase_rls_hardening_3_8.sql` | סוגר את 5 הקריטיים של ה-RLS | **כן** |
| 2 | `supabase_parent_consent.sql` | מערכת הסכמת ההורים | **כן** |
| 3 | `supabase_consent_enforcement.sql` | אכיפת החסימה בשרת | **כן** — דורש 1+2 |
| 4 | `supabase_hardening_medium_3_8.sql` | הקשחות + 13 אינדקסים | מומלץ — דורש 1+2 |
| 5 | `supabase_private_media.sql` | מדיה פרטית | **כן** — קרא את האזהרה למטה |

**אל תדלג ואל תשנה סדר.** קובץ 3 בודק שהפונקציות מ-1 ו-2 קיימות; אם לא, הוא לא
יוצר כלום ומדפיס אזהרה במקום להיכשל.

כל קובץ בטוח להרצה חוזרת. אם משהו נכשל באמצע — תקן והרץ את אותו קובץ שוב.

---

## שלב 5 — המדיה: שתי הרצות נפרדות

הקובץ מחולק בבאנר. **הרץ קודם רק את החלק שמעל הבאנר** (מדיניות הקריאה).
זה בטוח לגמרי — ה-bucket עדיין ציבורי והכל ממשיך לעבוד.

אחר כך תיכנס לאתר ותוודא שהתמונות עולות (אווטארים, תמונות בתרגילים, פיד הקהילה).
הקוד שכבר באוויר מגיש אותן ב-Signed URL, וזה עובד גם על bucket ציבורי.

**רק כשראית שהתמונות תקינות** — הרץ את השורה האחרונה שמתחת לבאנר:

```sql
update storage.buckets set public = false where id = 'media';
```

אם משהו נשבר, החזרה מיידית:

```sql
update storage.buckets set public = true where id = 'media';
```

---

## אחרי ההרצה — בדיקות

```sql
select * from public.schema_migrations order by ran_at desc limit 6;
```

חמשת הקבצים אמורים להופיע.

```sql
select tablename, policyname from pg_policies
where schemaname = 'public' and qual = 'true';
```

**התוצאה אמורה להיות ריקה.** כל שורה כאן היא טבלה שכל משתמש רשום יכול לקרוא
במלואה — בדיוק הבאג הקריטי מס' 1.

ואז תיכנס לאתר ותבדוק שהכל עובד: בית המאמן, הסגל, ספריית התרגילים, הקהילה, ההודעות.

---

## שני דברים שרק אתה יכול לעשות — לא ניתן מהקוד

**1. הגדרות Supabase Auth** (Authentication → Sign In / Providers → Password):
מינימום 8 תווים, דרישות מורכבות, ו-Leaked password protection.
וב-Attack Protection: CAPTCHA והגבלת קצב על OTP.
היום המינימום הוא 6 תווים ללא בדיקות — בחשבונות של קטינים זה בסיס להשתלטות בניחוש.

**2. מפתח ה-Google/YouTube.** המפתח הישן היה בהיסטוריית הגיט. אם עוד לא סובב —
תסובב אותו ב-Google Cloud Console, ותגביל אותו ל-YouTube Data API v3 בלבד עם
referrer של `courtsideil.vercel.app` ותקרת מכסה יומית.

---

## מה שנשאר פתוח ביודעין

**אין אימות טכני שמי שמאשר את ההסכמה הוא ההורה.** אין בפרויקט תשתית לשליחת
מיילים, ולכן הקישור עובר דרך הקטין (וואטסאפ/העתקה) — כלומר הוא יכול לפתוח אותו
בעצמו. מה שכן נסגר: אי אפשר לדרוס סירוב מפורש של הורה, אי אפשר לחדש הסכמה
שההורה ביטל, ואי אפשר לשנות את מייל ההורה אחרי שניתנה החלטה.

אכיפה אמיתית דורשת Edge Function ששולחת את הקישור למייל של ההורה — זה מצריך
חשבון אצל ספק מייל (Resend או דומה). `SECURITY.md` מצהיר את המצב הזה במפורש
במקום להבטיח אכיפה שלא קיימת.

**וכל הנוסחים המשפטיים עדיין דורשים עורך דין לפני גבייה או השקה רחבה.**
