# הרצת ה-SQL — 3.8.2026

הקוד באוויר. **שלושה קבצים כבר הרצת** (1, 1ב, 2) — נשארו חמישה.

עד שתריץ אותם, האפליקציה עובדת כמו קודם (כל הקוד החדש נופל אחורה כשהעמודה
או ה-RPC חסרים), אבל החורים שהדוח מצא נשארים פתוחים.

מריצים ב-**Supabase → SQL Editor → New query**, קובץ אחד בכל פעם, לפי הסדר.

---

## מה כבר רץ ✅

| # | קובץ | מה נסגר |
|---|---|---|
| 1 | `supabase_rls_hardening_3_8.sql` | 5 הקריטיים של ה-RLS |
| 1ב | `supabase_rls_gaps_3_8.sql` | 5 מדיניות `USING(true)` ששרדו — ובראשן שער אישור הסרטונים לקטינים, שמעולם לא עבד |
| 2 | `supabase_parent_consent.sql` | מערכת הסכמת ההורים |

---

## מה נשאר להריץ

| # | קובץ | מה זה נותן | חובה? |
|---|---|---|---|
| 3 | `supabase_consent_manage_fix.sql` | קישור ניהול חוזר להורה + `admin_set_consent` | **כן** — סוגר מבוי סתום |
| 4 | `supabase_admin_requests.sql` | ערוץ פנייה לאדמין + החלפת הורה רשום | **כן** |
| 5 | `supabase_consent_enforcement.sql` | אכיפת ההגבלות בשרת | **כן** — דורש 1 ו-2 |
| 6 | `supabase_hardening_medium_3_8.sql` | הקשחות + 13 אינדקסים | מומלץ — דורש 1 ו-2 |
| 7 | `supabase_private_media.sql` | מדיה פרטית | **כן** — קרא את האזהרה למטה |

כל קובץ בטוח להרצה חוזרת. אם משהו נכשל — תקן והרץ את אותו קובץ שוב.

**אחרי כל קובץ תסתכל שאין ERROR אדום בסוף.** שורות `NOTICE` ירוקות הן תקינות
ומכוונות — הקבצים מדווחים מה הם עושים.

---

## קובץ 7 — המדיה: שתי הרצות נפרדות

הקובץ מחולק בבאנר. **הרץ קודם רק את החלק שמעל הבאנר** (מדיניות הקריאה).
בטוח לגמרי — ה-bucket עדיין ציבורי והכל ממשיך לעבוד.

אחר כך תיכנס לאתר ותוודא שהתמונות עולות (אווטארים, תמונות בתרגילים, פיד
הקהילה). הקוד שבאוויר מגיש אותן ב-Signed URL, וזה עובד גם על bucket ציבורי.

**רק כשראית שהתמונות תקינות** — הרץ את השורה האחרונה שמתחת לבאנר:

```sql
update storage.buckets set public = false where id = 'media';
```

אם משהו נשבר, החזרה מיידית:

```sql
update storage.buckets set public = true where id = 'media';
```

---

## אחרי הכל — בדיקות

```sql
select * from public.schema_migrations order by ran_at desc limit 8;
```

```sql
select tablename, policyname from pg_policies
where schemaname = 'public' and qual = 'true';
```

**חייב לחזור ריק.** כל שורה כאן היא טבלה שכל משתמש רשום קורא במלואה.

ואז בדיקה חיה: בית המאמן, הסגל, תרגילים, קהילה, הודעות.

---

## מה שרק אתה יכול לעשות

**1. הגדרות Supabase Auth** (Authentication → Sign In / Providers → Password):
מינימום 8 תווים, דרישות מורכבות, Leaked password protection.
וב-Attack Protection: CAPTCHA והגבלת קצב על OTP.
היום המינימום 6 תווים ללא בדיקות — בחשבונות של קטינים זה בסיס להשתלטות בניחוש.

**2. מפתח Google/YouTube.** היה בהיסטוריית הגיט. אם לא סובב — סובב עכשיו,
והגבל ל-YouTube Data API v3 עם referrer של `courtsideil.vercel.app` ותקרה יומית.

---

## המודל שנבחר: המאמן כשומר סף

שחקן נכנס עם קוד קבוצה. קטין שההורה שלו טרם אישר **לא נחסם** — הוא נכנס,
עורך פרופיל, ומצטרף לקבוצה. מה שחסום לו: תמונות, קהילה, צ'אטים, הודעות.
המאמן רואה במסך אישור הבקשות אם ההורה אישר, ומאשר בעיניים פקוחות.

`suspended` (הורה ביטל) נשאר נעול — אבל גם משם אפשר לפנות לאדמין.

---

## מה שנשאר פתוח ביודעין

**אין אימות טכני שמי שמאשר הוא ההורה.** אין תשתית מייל, ולכן הקישור עובר
דרך הקטין. מה שכן נסגר: אי אפשר להסיט את הקישור למייל אחר, אי אפשר לחדש
הסכמה שההורה ביטל, ואי אפשר לדרוס סירוב על מדיה ציבורית.

הפיצוי על זה הוא המודל שבחרת — המאמן מכיר את המשפחה. אכיפה טכנית מלאה
דורשת Edge Function ששולחת את הקישור למייל ההורה, וזה מצריך ספק מייל.

**וכל הנוסחים המשפטיים דורשים עורך דין לפני גבייה או השקה רחבה.**
