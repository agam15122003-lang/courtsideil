# CourtSide — האנדוף 4.8.2026

מסמך מעבר לצ'אט חדש. הסשן הזה התחיל כסקירה לפני עטיפה למובייל, והפך
לשיפוץ אבטחה ופרטיות מלא. **הכל באוויר, וכל המיגרציות רצו בפרודקשן.**

---

## 0) לקרוא קודם

| קובץ | למה |
|---|---|
| `CLAUDE.md` | קונבנציות מחייבות. index.css append-only, `npm run verify` אחרי כל שינוי, פורט 5173 |
| `AUDIT_3.8.md` | דוח הסקירה — 108 ממצאים, מדורג, עם תוכנית פעולה ב-4 דליים |
| `SECURITY.md` | מודל האבטחה הנוכחי, מעודכן לסוף הסשן |
| `הרצת_SQL_3.8.md` | צ'קליסט ההרצה + מה שנשאר לבעלים |

**איך לדבר עם הבעלים:** עברית פשוטה, בלי ז'רגון. הוא מפעיל את המוצר ומריץ
SQL בעצמו אבל אינו מהנדס. מונחים כמו bucket / RLS / policy / upsert לא
מעבירים לו מידע. "כל אחד באינטרנט יכול לפתוח את התמונה" עובד; "ה-bucket
ציבורי" לא. **צעד אחד בכל פעם**, ולפני כל פעולה הרסנית — גם שורת הביטול.

---

## 1) מה נעשה בסשן

**16 קומיטים, `bd4408c` → `7e7ebe9`.** 14 מיגרציות חדשות, 5 קבצי פרונט חדשים,
~40 קבצים ששונו.

### הפערים הקריטיים שנסגרו

1. **`profiles` עם `USING(true)`** — כל משתמש רשום יכול היה לשלוף את מאגר
   כל הקטינים. הוחלף ב-`id = auth.uid() OR is_admin() OR role='coach' OR shares_team_with(id)`.
2. **הודעות פרטיות ללא בדיקת קשר** — כל מבוגר יכול היה לפנות לכל ילד.
3. **הסכמת ההורה נחתמה בידי הקטין** — הוחלף במערכת אמיתית (למטה).
4. **תמונות פנים של ילדים בכתובת ציבורית** — נפתר בשורש: **לשחקנים אין
   תמונת פרופיל בכלל.** החלטת הבעלים, ונכונה — מה שלא נאסף לא דולף.
5. **מחיקת חשבון הייתה הבטחה ריקה** — `admin_execute_deletion` מוחקת בפועל.

### מערכת הסכמת ההורים

`consent_documents` (נוסחים מגורסנים) · `guardians` · `consent_requests`
(רק sha256 של הטוקן) · `consents` (**append-only**, טריגר חוסם UPDATE/DELETE — זו הראיה).

ארבע קטגוריות: `basic` (חובה) · `media_team` · `media_public` · `marketing`.

הזרימה: קטין נרשם → הטריגר מחשב גיל בשרת → `approval_status='pending_parent'`
→ הקטין מייצר קישור ומעביר להורה בוואטסאפ → ההורה פותח `#/consent/<token>`
ומחליט על כל קטגוריה בנפרד → `basic=granted` מפעיל את החשבון.

### המודל שנבחר: המאמן כשומר סף

שחקן נכנס עם קוד קבוצה. **קטין שההורה טרם אישר אינו נחסם** — הוא נכנס, עורך
פרופיל, ומצטרף לקבוצה (זו הדרך שלו להגיע למאמן). חסום לו: תמונות, קהילה,
צ'אטים, הודעות, RSVP, דיווחי מאמץ. המאמן רואה במסך אישור הבקשות אם ההורה
אישר, ומקבל דיאלוג מפורש לפני אישור קטין שטרם אושר.

`suspended` (הורה ביטל) נשאר נעול — אבל גם משם אפשר לפנות לאדמין.

---

## 2) ארבעה לקחים שעלו רק מהרצה אמיתית

אף אחד מהם לא נתפס ב-build, בביקורת האדוורסרית או בקריאת קוד. **שווה לזכור
אותם לפני כל מיגרציה בפרויקט הזה.**

**א. מדיניות permissive מתאחדות ב-OR.** מדיניות `USING(true)` ששרדה מבטלת
בשקט כל שער שנוסף אחריה. ככה שער אישור הסרטונים לקטינים **מעולם לא עבד** —
מדיניות בשם `videos read all` שנוצרה ידנית בדשבורד (לא קיימת באף קובץ SQL)
ביטלה אותו. הבדיקה:
```sql
select tablename, policyname from pg_policies
 where schemaname='public' and qual='true';
```
**החריגה המכוונת היחידה: `consent_documents`** — ההורה חייב לקרוא את המסמך
בלי חשבון. כל שורה אחרת = חור.

**ב. שער חדש לא חל למפרע.** `approval_status` נוסף עם `default 'active'`,
והטריגר רץ רק ב-INSERT/UPDATE — כך שכל קטין שכבר היה רשום נשאר `active`.
תוקן בסעיף 10ג של `supabase_parent_consent.sql` (UPDATE ריק שמפעיל את הטריגר).

**ג. `upsert` דורש SELECT ברמת הטבלה.** PostgREST מתרגם ל-`INSERT ... ON
CONFLICT DO UPDATE`, וכש-RLS פעיל פוסטגרס קורא את השורה המתנגשת כדי לאכוף
את מדיניות ה-UPDATE. `privacy4` ביטל SELECT ברמת הטבלה בכוונה → **כל שמירת
פרופיל נשברה בפרוד**. הפתרון: `update` ואם 0 שורות → `insert` (ProfileForm).
**אל תוסיף `grant select on profiles` — זה מבטל את privacy4 כולו.**

**ד. הרשאות עמודה הן שכבת עומק, לא ההגנה.** נעילת עמודות על `profiles`
הפילה את הפיצ'ר שהיא הגנה עליו. ההגנה האמיתית היא הטריגר, ואומת חי שהוא
מחזיק. הבלוק בוטל עם נימוק מלא בקובץ.

---

## 3) מצב נוכחי

- **קוד:** `main` = `redesign` = `7e7ebe9`, נפרס אוטומטית ל-Vercel
- **מסד:** כל 14 המיגרציות רצו. `select * from public.schema_migrations order by ran_at desc;`
- **אחסון:** הבעלים סגר את bucket `media`. תמונות תרגילים נשארות פתוחות
  לאנונימי (דף התרגיל הציבורי), השאר רק למחוברים
- **סיסמאות:** בסופאבייס — מינימום 8 + `Lowercase, uppercase letters and digits`.
  בקוד — בודק אחד ב-`constants.js` מיושר להגדרה הזו
- **מפתח YouTube:** מוגבל ל-API אחד ולדומיינים. הדומיין הישן (netlify) נמחק,
  `courtsideil.vercel.app` נוסף. **לא הוחלף בכוונה** — ההגבלות מספיקות

---

## 4) מה נשאר — לפי סדר חשיבות

### חוסם השקה

**עורך דין.** המנגנונים בנויים והנוסחים עודכנו לתאר את המציאות, אבל אף אחד
מהם לא נבדק משפטית. חובה לפני גבייה או קהל רחב.

### לאמת — ריק. שני הפריטים נסגרו ב-4.8.2026

**חיפוש הסרטונים ביוטיוב — עובד.** היה 100% שגיאות בגלל הדומיין הישן; תיקון
ההגבלה פתר. `VITE_YOUTUBE_API_KEY` מוגדר ב-Vercel כראוי — סריקת ה-bundle לא
מצאה אותו כי Vite מזריק אותו בזמן build.

**`supabase_retention_schedule.sql` — התזמון תפס.** ההנחה שאין pg_cron
בפרויקט הופרכה: היא מותקנת (1.6.4), והמשימה `courtside-retention-purge`
רשומה ופעילה על `25 3 * * *` (UTC). הניקוי היומי רץ, סופר, ולא מוחק —
המחיקה בפועל עדיין מושבתת עד לאישור עורך דין.

בדיקה חוזרת — `admin_retention_status()` דורשת חשבון מנהל מחובר ולכן
**נכשלת ב-SQL Editor**. שם משתמשים בשתי אלה:

```sql
select extname, extversion from pg_extension where extname = 'pg_cron';
select jobname, schedule, active from cron.job where jobname = 'courtside-retention-purge';
```

### פערים ידועים ומכוונים

**אין אימות טכני שמי שמאשר הוא ההורה.** אין תשתית מייל, ולכן הקישור עובר
דרך הקטין. נסגר מה שאפשר: אי אפשר להסיט את הקישור למייל אחר, אי אפשר לחדש
הסכמה שבוטלה, אי אפשר לדרוס סירוב. הפיצוי הוא מודל שומר הסף.
**הפתרון האמיתי: Edge Function ששולחת את הקישור למייל ההורה — דורש ספק מייל.**

**הריפו פומבי, ובהיסטוריה יש טוקן ישן** (`public/__devlogin.html`, קומיטים
`87e1ef1`–`566a697`). הסשן בוטל. שכתוב היסטוריה דורש force-push — החלטת הבעלים.

**לוג הגישה מתעד שינויים, לא קריאות.** תיעוד קריאות דורש שכל קריאה תעבור
RPC — שינוי גדול. כתוב במפורש ב-`supabase_audit_retention.sql`.

### נבנה חלקית — עטיפה לאנדרואיד (4.8.2026)

Capacitor 8.5 מותקן, `android/` בריפו. `il.courtside.app` · "Courtside.il" ·
minSdk 24 · compileSdk 36 · AGP 8.13 / Gradle 8.14.3. אייקון ומסך פתיחה
מיוצרים מהלוגו ע"י `scripts/make-app-assets.mjs` + `@capacitor/assets`.
זרימת העבודה: `npm run app:sync` ואז `npm run app:open`.

**אחסון הטוקן עבר ל-Preferences — אבל רק בנייטיב.** ב-WebView ה-localStorage
מתפנה בלחץ אחסון ומנתק משתמשים באקראי. בדפדפן נשארת ברירת המחדל של
supabase-js **במכוון**: Preferences מוסיף קידומת למפתח, ומעבר גורף היה
מנתק בבת אחת כל משתמש מחובר באתר החי.

**`android/.gitignore` — שורות ה-keystore הופעלו ידנית.** התבנית משאירה
אותן כהערה; הריפו ציבורי, ומפתח חתימה שדולף אינו ניתן לביטול.

**מה שנשאר:** אין JDK ואין Android SDK על מכונת הפיתוח — בלי Android Studio
אי אפשר לייצר קובץ להתקנה. וכן: iOS (דורש macOS), הרשאות, וחשבונות החנויות.

### לא נבנה

**Push notifications** — דורש FCM + שרת ששולח.

**פיצול קבצי ענק** — `PlayerDashboard` ~2000 שורות, `Community` 1430,
`TrainingPlans` 1400. `index.css` ~22,500 שורות (append-only).

---

## 5) מלכודות בפרויקט

- **`index.css` append-only.** תיקון לכלל קיים = בלוק override מתוארך בסוף.
- **`supabase_hardening_medium_3_8.sql` מריץ לולאה** שמעניקה EXECUTE לכל
  פונקציית `prosecdef` ב-public. פונקציית עזר שאסור שתהיה קריאה למשתמש
  **חייבת להיות SECURITY INVOKER**. סוכן נפל בזה.
- **מסמך `blob:` יורש את ה-CSP של הדף שיצר אותו.** ככה הדפסת התוכנית נשברה
  אחרי המעבר לפונטים מקומיים.
- **קוד חייב לשרוד מסד שטרם הריץ מיגרציה.** `src/consent.js` מתרגם
  PGRST202/42883/42P01 ל-`{notDeployed:true}`.
- **`npm run verify` אחרי כל שינוי.** אין טסטים אוטומטיים.
