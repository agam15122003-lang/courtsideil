# הרצת ה-SQL — עולם המשחק (האתגר והניחושים) · 12.8.2026

זה המסמך של הגל החדש: **המגרש המשחקי** — האתגר השבועי והניחושים.

מריצים ב-**Supabase → SQL Editor → New query**, קובץ אחד בכל פעם.
כל קובץ בטוח להרצה חוזרת. אם משהו נכשל — תקן והרץ את אותו קובץ שוב.
**אחרי כל קובץ תסתכל שאין ERROR אדום בסוף.** שורות `NOTICE` ירוקות הן תקינות ומכוונות.

---

## מצב נוכחי (12.8.2026, 18:35)

**בדיקת הקרקע בוצעה — חמש מתוך חמש תקינות:**

| הבדיקה | התוצאה | משמעות |
|---|---|---|
| מחסן `media` ציבורי? | `false` | ✅ פרטי. מותר להעלות סרטון של קטין |
| הרשאות `qual='true'` | `consent_documents` בלבד | ✅ אין הרשאה פתוחה מפתיעה |
| `is_admin` חשופה ל-anon? | `false` | ✅ **ולכן מדיניות ה-Storage בקובץ 41 חייבת להתפצל לשתיים** — מדיניות אחת שקוראת ל-`is_admin()` עבור `anon` הייתה מפילה כל קריאה אנונימית ומעלימה את התמונות בדף התרגיל הציבורי |
| `pg_cron` | `1.6.4` | ✅ המקצב השבועי ירוץ אוטומטית |
| מגבלת גודל למחסן | לא הוגדרה | נגדיר בקובץ 41 |

**רצו בהצלחה:** `supabase_game_core_12_8.sql` (18:33) · `supabase_game_legal_12_8.sql` (18:34).
אומת: 7 טבלאות חדשות, 7 חוקי ניקוד, עברית תקינה במסד, שער הפרס קיים,
טריגר «מאמן קטין» פעיל, קטגוריית `competition` נוספה, אפס פונקציות בלי `search_path`.

⚠ **המתג `require_coach_birthdate` עדיין כבוי** — מדליקים רק אחרי שמסך
ההרשמה יודע לאסוף תאריך לידה ממאמן.

---

## שלב 0 — בדיקת קרקע (בוצע ✅)

שש שאילתות. הן **רק קוראות** ולא משנות כלום. תריץ, ותדביק לי בחזרה את מה שחזר.
לכל אחת כתוב מה התשובה הטובה ומה עושים אם התשובה אחרת.

### 0.1 — האם מחסן הקבצים פרטי?

```sql
select id, public, file_size_limit, allowed_mime_types
  from storage.buckets where id = 'media';
```

**מה אנחנו רוצים לראות:** `public = false`.

⛔ **אם חזר `public = true` — עוצרים כאן.** זה אומר שכל קובץ במחסן פתוח לכל אדם
באינטרנט שיודע את הכתובת. אסור להעלות סרטון של ילד למחסן ציבורי. במקרה כזה יש
שורה אחת שמריצים כדי להפוך אותו לפרטי — היא מחכה בסוף `supabase_private_media.sql`,
ואני אתן לך אותה בנפרד עם הסבר. **אחרי** ההיפוך בודקים שהתמונות באתר עדיין נטענות.

`file_size_limit` ו-`allowed_mime_types` כנראה יחזרו ריקים — זה בסדר, אנחנו נגדיר
אותם בקובץ 41.

### 0.2 — יש הרשאה פתוחה לרווחה איפשהו?

```sql
select tablename, policyname
  from pg_policies
 where schemaname = 'public' and qual = 'true';
```

**מה אנחנו רוצים לראות:** רק `consent_documents` (נוסח ההסכמה — הוא אמור להיות
קריא לכולם).

⚠ אם חזרה עוד טבלה — תגיד לי מה שמה לפני שממשיכים. זו הרשאה שמאפשרת לכל
משתמש מחובר לקרוא את כל הטבלה.

### 0.3 — האם `is_admin` חשופה למשתמש אנונימי?

```sql
select has_function_privilege('anon', 'public.is_admin()', 'execute') as anon_can_run;
```

**מה אנחנו רוצים לראות:** `false`.

זה נשמע טכני, אבל יש לזה משמעות מעשית: אם התשובה `true`, מדיניות ההרשאות החדשה
שנכתוב על מחסן הקבצים תיבנה אחרת. תדביק לי את התשובה ואני אתאים.

### 0.4 — האם המתזמן מותקן ופעיל?

```sql
select extname, extversion from pg_extension where extname = 'pg_cron';
select jobname, schedule, active from cron.job;
```

**מה אנחנו רוצים לראות:** שורה אחת עם `pg_cron`, ורשימת משימות מתוזמנות.

המתזמן הוא מה שיפתח את האתגר בשלישי, ינעל בשבת, ויכריז את המנצח במוצ"ש — בלי
שתצטרך לעשות כלום. אם הוא לא מותקן, נבנה את אותו דבר עם כפתור שאתה לוחץ.
זה עובד, זה פשוט פחות נוח.

### 0.5 — מה כבר רץ עד היום?

```sql
select filename, ran_at from public.schema_migrations order by ran_at desc limit 10;
```

זה נותן לי את נקודת ההתחלה המדויקת. הקבצים החדשים ממשיכים משם.

### 0.6 — מי מנהל היום?

```sql
select id, first_name, last_name, is_admin
  from public.profiles where is_admin = true;
```

**מה שצריך לקרות אחרי זה: להוסיף מנהל שני.**

למה זה בשלב 0 ולא אחר כך: תור אישור הסרטונים הוא **אתה בלבד**. ילד מעלה קליפ ביום
חמישי בערב, אתה בטיול/חולה/בלי קליטה, והקליפ לא מאושר עד מוצ"ש — הוא לא נכנס
לטבלה, והילד חושב שהמערכת התעלמה ממנו. עם מנהל שני זה לא קורה.

תמר היא הבחירה המתבקשת (היא כבר בצוות נערים א'). קודם שתיצור חשבון רגיל
באפליקציה, ואז תריץ עם המייל שלה:

```sql
update public.profiles set is_admin = true
 where id = (select id from auth.users where email = 'המייל-של-תמר');
```

---

## שלב 1 — הקבצים החדשים (עוד לא מוכנים)

הטבלה הזו תתמלא ככל שהקבצים ייכתבו. הסדר מחייב — כל קובץ מסתמך על מה שלפניו.

| # | קובץ | מה הוא נותן | סטטוס |
|---|---|---|---|
| 38 | `supabase_game_core_12_8.sql` | מנוע הנקודות, הטבלאות, שם התצוגה, שערי הכניסה | ✅ רץ 12.8 |
| 39 | `supabase_game_legal_12_8.sql` | הסכמת «השתתפות בתחרות», שער עורך הדין, חסימת «מאמן קטין» | ✅ רץ 12.8 |
| 40 | `supabase_game_challenges_12_8.sql` | האתגרים, ההגשות, תור האישור, ההכרעה | ✅ רץ 12.8 (ערב, אחרי תיקון סדר הפונקציות) |
| 41 | `supabase_game_media_12_8.sql` | מחסן הסרטונים והמדיניות עליו | ✅ רץ 12.8 — כל המדיניות נוצרה, בלי צורך בהזנה ידנית |
| — | `supabase_game_quiz_12_8.sql` | חידונים ודו-קרבות: המאגר הנעול, ניסיונות, ניקוד בשרת | ✅ רץ 12.8 |
| — | `supabase_game_quiz_bank_12_8.sql` | 320 שאלות מאומתות. **חייב אחרי quiz** — הוא מכיל את התשובות | ✅ רץ 12.8 (בניסיון השני — «Failed to fetch» ראשון היה נפילת רשת של הדשבורד, לא שגיאת SQL) |
| — | `supabase_game_standing_16_8.sql` | «המקום שלי» סופר את אותה אוכלוסייה כמו הטבלה | ⏳ מוכן — **מחכה להרצה** |
| 42 | `supabase_game_predictions_12_8.sql` | הליגות, המחזורים, המשחקים, הניחושים | טרם נכתב — לקראת פתיחת העונה |
| 43 | `supabase_game_ops_12_8.sql` | המקצב השבועי האוטומטי | טרם נכתב |

### `supabase_game_standing_16_8.sql` — מה הוא מתקן (16.8.2026)

**מה שבור היום:** כרטיס «המקום שלי» וטבלת האתגר סופרים אוכלוסייה שונה.
הטבלה מציגה רק שחקנים רשומים (חשבון פעיל, לא חסום, תפקיד «שחקן», יש שם
פרטי), והכרטיס דירג את **כולם** — כולל קטין שממתין לאישור הורה. לכן אפשר
לראות «מקום 7 מתוך 14» מעל טבלה שיש בה פחות שחקנים.

**מה הקובץ עושה:** מיישר את הכרטיס לטבלה — אותו מסנן בדיוק. ובנוסף:
מי שעדיין אינו רשום לטבלה **ימשיך לראות את הנקודות שצבר** (המסך כבר
אומר לו «עוד לא בטבלה» במקום מקום מומצא).

**מה זה משנה על המסך:** אצל שחקן רשום — כלום, חוץ מזה ששני המספרים
מסכימים. אצל שחקן שאינו רשום — המקום יהפוך ל־«—» במקום מספר שלא היה
נכון. הפרונט לא דורש שינוי; הקובץ בטוח להרצה חוזרת.

**איפה להריץ:** אחרי `supabase_game_core_12_8.sql` (כלומר עכשיו).
הבדיקות אחרי ההרצה כתובות בסוף הקובץ עצמו.

---

⚠ **41 חייב לרוץ בנפרד מ-40**, וגם אם הוא נכשל — זה לא הורס את מה שכבר רץ. אם
הוא מסיים עם `NOTICE` שאומר «אין הרשאה», זה תרחיש מוכר: תדביק לי את מה שהוא
הדפיס ואני אתן לך את השורות להדבקה ידנית בדשבורד.

---

## שלב 2 — בדיקות אחרי כל הרצה

אחרי כל קובץ, הרץ את השורות האלה ותוודא שהן חוזרות כמצופה:

```sql
-- מה רץ עכשיו
select filename, ran_at from public.schema_migrations order by ran_at desc limit 6;

-- אין הרשאה פתוחה חדשה (שוב: רק consent_documents)
select tablename, policyname from pg_policies
 where schemaname = 'public' and qual = 'true';

-- כל הפונקציות החדשות מוגנות (חייב לחזור ריק)
select proname from pg_proc p
  join pg_namespace n on n.oid = p.pronamespace
 where n.nspname = 'public' and p.prokind = 'f' and p.proconfig is null;
```

**השורה השלישית חשובה במיוחד.** אם חזר ממנה שם של פונקציה — משהו נכתב לא נכון
ויש לתקן לפני שממשיכים. תדביק לי את השם.

---

## שלב 3 — מה שרק אתה יכול לעשות (לא בקוד)

| מה | למה זה חוסם | מתי |
|---|---|---|
| **עורך דין מאשר את החבילה** | בחרת להשיק פתוח לכולם **ועם פרס** — הצירוף הזה מחייב תקנון פומבי וחוות דעת לפני האתגר הראשון | לפני האתגר הראשון |
| **תקנון פומבי באתר** | מי מפעיל, מי רשאי, איך שופטים, שובר שוויון, ערך הפרס, שהפרס לקטין נמסר להורה, ומסלול השגה | יחד עם עורך הדין |
| **מנהל שני** | ראו 0.6 | עכשיו |
| **אישור בנק 12 האתגרים** | אני אכין טיוטה באקסל, אתה עובר ומתקן | לפני ההשקה |

⚠ **המערכת תסרב פיזית לפתוח אתגר עם פרס** כל עוד לא נרשמו במסד תאריך אישור
עורך הדין וקישור לתקנון. זה לא נוהל שאפשר לשכוח — זו חומה. אם תנסה, תקבל שגיאה.
