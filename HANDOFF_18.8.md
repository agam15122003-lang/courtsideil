# CourtSide — האנדוף 18.8.2026

מסמך מעבר לצ'אט חדש. הסשן הזה כלל **שני פיצ'רים גדולים** — «המחברת המלאה»
ו«תיק שחקן» — ובנוסף מסמך ניהול שנבנה מול המסד עצמו וחשף מיגרציה שלא רצה.

**הכול נדחף ל-`main` ובאוויר.** אין ענפים פתוחים. שתי מיגרציות חדשות רצו
בפרודקשן ואומתו; אחת ישנה (13.8) התגלתה כלא־רצה — ראו §5.

---

## 0) לקרוא קודם, בסדר הזה

| קובץ | למה |
|---|---|
| **`ניהול_הפרויקט.md`** | **המסמך הראשון.** מה להריץ, מה להחליט, מה נשאר — מאומת מול המסד |
| `CLAUDE.md` | קונבנציות מחייבות: `index.css` append-only, `npm run verify` אחרי כל שינוי, SQL אידמפוטנטי |
| `DESIGN.md` | שפה עיצובית — RTL מלא, אייקונים רק מ-lucide, ≥44px, שלושה מצבים לכל מסך |
| `HANDOFF_6.8.md` | הסשן הקודם (אנדרואיד, לו״ז, אימון אישי) |
| `הרצת_SQL_18.8.md` | דף ההרצה של הגל הזה |

**איך לדבר עם הבעלים:** עברית פשוטה, בלי ז'רגון, **צעד אחד בכל פעם**, ותמיד
לתת את שורת הביטול. הוא מפעיל את המוצר ומריץ SQL בעצמו — אך אינו מהנדס.
הוא לא פותח דפים מפורסמים (artifacts); מגישים לו **קבצים**.

⚠ **הוא צודק כשהוא אומר «לא מובן».** בסשן הזה הוא פסל את מסך «עץ המועדון»
ואת «סבב הדירוג» בגרסתם הראשונה — ובשני המקרים הוא צדק, והגרסה השנייה
טובה בהרבה. אל תתגונן; תבנה מחדש.

---

## 1) מה נבנה — «המחברת המלאה»

תוכנית אימון היא **דף מחברת אחד** במקום חלקים ממוספרים.

| חלק | קובץ |
|---|---|
| העורך | `src/PlanNotebook.jsx` |
| טקסט על שורות + שכבת כתב יד | `src/NotebookBody.jsx` |
| ציור חופשי (עט/מחק, משותף) | `src/ink.jsx` |
| מגרש קטן שמציירים עליו | `src/MiniCourt.jsx` |
| הדף לקריאה (הדפסה/קהילה/שחקן) | `src/PlanSheet.jsx` |
| המסד | `supabase_notebook_18_8.sql` ✅ רץ |

**החלטות של הבעלים שמעצבות את הפיצ'ר:**
- אין שחזור טיוטה אוטומטי. יציאה בלי לשמור = השינויים אבדו (יש אזהרה).
- אין חלקים — המאמן ממספר בעצמו אם ירצה.
- תרגיל מהספרייה נכנס **בנקודת הכתיבה**: שם + תוכן, ונשאר מקושר לתוכנית.
- נוכחות בתחתית הדף: כולם «מגיע», מסמנים רק חריגים + סיבה.
- המגרשים **לאורך** (portrait), בצד שמאל; בטלפון — בתחתית.
- כתב היד נשמר כציור, לא הופך לטקסט (זיהוי עברית לא אמין; הבעלים אישר).

---

## 2) מה נבנה — «תיק שחקן»

התיק עובר עם השחקן משנה לשנה.

| חלק | קובץ |
|---|---|
| שלושת המסכים | `src/PlayerDossier.jsx` |
| כל הקריאות למסד | `src/dossierApi.js` |
| המסד | `supabase_dossier_18_8.sql` ✅ רץ |

**המודל:** `dossier_people` = אדם אחד לכל שחקן במועדון. `team_players.person_id`
תולה עליו כל שורת־קבוצה של כל שנה. הדירוגים/המדידות/ההערות תלויים על **האדם**,
ולכן הגרף ממשיך גם כשהשחקן עולה שכבה.

**כלל ההרשאות שהבעלים קבע — זה ההיגיון של הפיצ'ר:**
- מאמן רואה את הקבוצות שלו.
- מנהל מועדון/מקצועי רואים **רק מאמנים שצורפו לעץ** (`club_roles.role='coach'`).
  מאמן שלא צורף — פרטי לחלוטין, גם אם רשם את שם המועדון בפרופיל.
- מאמן אחר — רק בגישה מפורשת. שחקנים והורים — כלום. אדמין המערכת — גם לא.
- הכול בפונקציה אחת: `dossier_can_see` / `dossier_can_edit`. הוספת תפקיד
  בעתיד = שינוי שם, לא ב-20 מדיניות.

**המסך פתוח כרגע רק ל-`profile.is_admin`** (`src/Dashboard.jsx:622,821`).
זו החלטה פתוחה — ראו `ניהול_הפרויקט.md` §3.

---

## 3) ⚠ מלכודות — כל אחת עלתה זמן בסשן הזה

1. **RLS רקורסיבי.** מדיניות על `club_roles` שקוראת ל-`club_roles` מפילה את
   Postgres ב-«infinite recursion detected in policy». הפתרון: פונקציית
   `security definer` (`is_club_manager`). זה יחזור בכל טבלת תפקידים.

2. **יחידות הדיו.** כתב יד נשמר עם **x יחסי (0..1000) ו-y בפיקסלים** —
   כי שורות המחברת הן 30px קבועים בכל רוחב מסך. אם תשנה ל-viewBox אחיד,
   כתב היד יזוז מהשורות בטלפון ובהדפסה. `preserveAspectRatio="none"` הוא
   חלק מהפתרון, לא באג.

3. **עט שנתקע.** החלפת כלי באמצע קו (עט על טאבלט + אצבע על הסרגל) מנתקת
   את ה-handlers, ה-pointerup לא מגיע, והעט «תפוס» לנצח. `useInkTool`
   מאפס את המחווה ב-effect כש-`drawing` נכבה.

4. **לחיצה שנבלעת בפתיחה ראשונה.** מזהה האדם נוצר רק אחרי RPC; לחיצה על
   דירוג באותה שנייה נבלעה בשקט. `setValue` מקבל גם `rosterId` ופותח תיק
   בעצמו. אל תסמוך על prop שמגיע בטיימינג.

5. **`.court--full` בקסקייד.** הכלל נכתב **לפני** `.court` ולכן הפסיד לו;
   מגרש שלם קיבל יחס של חצי מגרש. תוקן עם סלקטור חזק יותר.

6. **בדיקת פריסה חייבת לעקוף מטמון.** `curl` רגיל מחזיר עותק ישן ומשקר.
   הדפוס: HTML טרי עם `Cache-Control: no-cache` → לחלץ את שם קטע ה-index →
   לחפש בו את שם הקטע של המסך → רק «נמצא» נחשב הצלחה.

7. **הערת `{/* */}` בין props ב-JSX** אינה חוקית ומפילה את הבנייה. קרה בסשן
   הזה ונדחף. **תמיד לקרוא את פלט `npm run verify` עצמו** — לא לסנן אותו
   ב-grep שעלול «להצליח» על שורת שגיאה.

---

## 4) איך בודקים חי בלי חשבון של הבעלים

- **מיגרציה רצה?** אין צורך בסשן: `select=<עמודה>` עם ה-anon key מחזיר
  `42703` אם העמודה חסרה, ו-`[]` אם קיימת (RLS פשוט לא מחזיר שורות).
  כך אימתתי 13 עמודות בשורה אחת.
- **יומן ההרצות:** `schema_migrations` (73 שורות). **זה מקור האמת** —
  מסמכי ההרצה מתיישנים, המסד לא.
- **מסך פנימי:** מייצרים session עם `POST /auth/v1/token?grant_type=password`
  (חשבון בדיקה של הבעלים), מזריקים ל-localStorage תחת
  `sb-nqfepcpkfvisvdviwqry-auth-token`, ומריצים Playwright מ-`node_modules`.
  ⚠ **לבטל את הסשן בסוף** (`POST /auth/v1/logout?scope=global`) ולמחוק את
  הקובץ. ⚠ **לא ליצור `public/__devlogin.html`** — הוא נכנס לגיט ודלף פעם.
- **נתוני בדיקה על שחקנים אמיתיים — למחוק אחרי.** בסשן הזה נכתבו דירוגים
  והערות על הסגל האמיתי ונמחקו בסוף.

---

## 5) מה נשאר לבעלים

**הפירוט המלא ב-`ניהול_הפרויקט.md`.** בקצרה, לפי דחיפות:

| # | מה | סוג |
|---|---|---|
| 1 | **`supabase_game_quiz_hardening_13_8.sql` מעולם לא רץ** — טבלת נקודות ניתנת לזיוף | להריץ |
| 2 | `v_frontend_ready` ב-`supabase_hardening_medium_3_8.sql` עדיין `false` — **הפרונט כבר מוכן** | להריץ |
| 3 | מינוי מנהל מועדון (שורה אחת) — בלעדיה מבנה ההרשאות של תיק שחקן ישן | להריץ |
| 4 | עורך דין: תקנון, פרסים, מדיניות פרטיות | להחליט |
| 5 | לפתוח את תיק שחקן לכל המאמנים? | להחליט |
| 6 | ניחושים (קובץ 42) ומקצב שבועי (43) — מעולם לא נכתבו | לבנות |
| 7 | מסך ניהול מועדון, עריכת קטלוג, השוואה לממוצע, הדפסת תיק | לבנות |

---

## 6) מצב טכני

- `main` = `origin/main`, אין ענפים פתוחים, הבנייה עוברת.
- 8 קומיטים ב-18.8: מהמחברת (`38d1117`) ועד תיקון הבנייה (`0f73323`).
- קבצים חדשים בסשן: `ink.jsx`, `NotebookBody.jsx`, `MiniCourt.jsx`,
  `PlanSheet.jsx`, `PlayerDossier.jsx`, `dossierApi.js`, `unsavedGuard.js`,
  `supabase_notebook_18_8.sql`, `supabase_dossier_18_8.sql`,
  `הרצת_SQL_18.8.md`, `ניהול_הפרויקט.md`.
- `PlanBuilder` הישן (בונה התוכניות בחלקים) **נמחק** מ-`TrainingPlans.jsx`.
- `unsavedGuard.js` — מנגנון כללי: מסך עם עבודה פתוחה עוצר מעבר מסך.
  היום רק המחברת משתמשת בו; כל מסך עם טופס ארוך יכול.

---

## 7) מה **לא** לעשות מחדש

- אל תבנה מחדש את התצוגה המוקדמת של תיק שחקן על נתוני דוגמה — היא אושרה
  והוחלפה בנתונים אמיתיים. העותק שמור ב-git history בלבד.
- אל תריץ שוב את הסקירה האדוורסרית של המחברת — 34 ממצאים טופלו; רק
  «אחידות מונחי הנוכחות» (מגיע/מאחר מול נוכח/איחר) נשאר במכוון.
- אל תשנה את סקאלת הדירוג ל-1–10 בלי לשאול — 1–5 היא החלטה מודעת.
- `session_effort.effort` הוא **1–10** ולא 1–5. המסך אומר «עומס מדווח».

---

*נכתב 18.8.2026 מול מצב המסד והקוד באותו יום.*
