# SECURITY.md — מצב האבטחה של CourtSide

עודכן: 4.8.2026 · נכתב בעקבות `AUDIT_3.8.md`, ומעודכן אחרי גל האבטחה והפרטיות של 4.8

CourtSide מחזיקה נתונים של **קטינים** (שחקנים מתחת לגיל 18): שם, גיל, טלפון הורה,
סטטוס בריאות, נוכחות והודעות. זו הסיבה שכל החלטת אבטחה כאן שמרנית יותר ממה
שאפליקציה דומה למבוגרים הייתה מצדיקה. המסמך מתאר **מה מוגן בפועל**, מה נאכף
בשרת מול מה שנאכף רק בלקוח, ומה מוגדר כסיכון מקובל.

**תמונת פרופיל של שחקן כבר לא נאספת בכלל** — ראו סעיף 5. זו ההחלטה היחידה בגל
הזה שלא הוסיפה שכבת הגנה אלא ביטלה את הצורך בה.

> מסמך הנדסי, לא ייעוץ משפטי. ההיבטים המשפטיים (תיקון 13, העברת מידע קטינים
> לחו"ל, נוסח ההסכמה, אורך תקופות השמירה) **טעונים בדיקת עורך דין** ומסומנים
> ככאלה גם כאן וגם ב-`public/privacy.html`.

---

## 1. דיווח על פרצה

מצאת בעיית אבטחה? כתוב ל-**agam15122003@gmail.com** (`CONTACT_EMAIL` ב-`src/constants.js`)
עם תיאור, שלבי שחזור והשפעה. אל תפתח issue ציבורי ואל תבדוק על חשבונות אמיתיים של
שחקנים. נשתדל להשיב תוך 72 שעות. הפרויקט אינו מציע תוכנית תגמול.

---

## 2. מודל האיום

| מי | מה מנסים למנוע |
|----|----------------|
| משתמש מחובר סקרן | שליפת מאגר הקטינים דרך קריאת REST ישירה עם ה-anon key, מחוץ למסכי האפליקציה |
| מבוגר זר | פנייה ישירה (DM) לקטין שאין איתו שום קשר, או כניסה למרחב השחקנים |
| קטין | כניסה לקהילת המאמנים, ואישור עצמי של הסכמה הורית |
| משתמש שנחסם | המשך פעילות עם JWT שכבר הונפק |
| גולש מנותק | דליפת מזהים ומטא-דאטה מהדפים הציבוריים |
| מי שרואה את הריפו | הפקת סוד שימושי מהקוד או מהיסטוריית ה-git |

**מחוץ למודל:** מאמן זדוני שאושר לקבוצה (הוא אמור לראות את שחקניו — ההגנה היא
מדיניות ולא טכנית), השתלטות על מכשיר המשתמש, ותקיפה על ספקי הענן עצמם.

---

## 3. מודל ההרשאות (RLS)

RLS פעיל על כל טבלה. אין service-role key בקוד הלקוח — האפליקציה מדברת עם המסד
אך ורק כ-`anon`/`authenticated`, ולכן **המדיניות היא ההגנה, לא המסך**.

הבסיס נכתב ב-`supabase_rls_hardening_3_8.sql` ומורכב מפונקציות עזר
`SECURITY DEFINER` (כדי לא ליצור רקורסיית RLS): `is_coach()`, `is_admin()`,
`is_banned()`, `shares_team_with()`, `is_active_user()`.
`supabase_rls_gaps_3_8.sql` סגר אחריו חמש מדיניות `USING(true)` ששרדו, ובראשן
שער אישור סרטוני הקטינים.

### מי רואה פרופיל של מי
```
profiles: id = auth.uid()  OR  is_admin()  OR  role = 'coach'  OR  shares_team_with(id)
```
* **מאמנים גלויים** — הם ממילא ספרייה ציבורית במוצר (`coach_directory`, מחברי תרגילים ופוסטים).
* **שחקן — לרוב קטין — נראה אך ורק למאמן שלו ולחברי הקבוצה שלו.** זה החלק שנועד למנוע שאיבת מאגר קטינים.
* **חסום רואה רק את עצמו.**
* `role` נעול אחרי היצירה (נסגרה גם העקיפה דרך `first_name is null`).
* עמודות רגישות (`birth_date`, טלפון) מחוץ ל-whitelist הקריאה; המשתמש מקבל אותן רק דרך `my_profile()`.

### הודעות
קריאה — שני הצדדים לשיחה בלבד. **כתיבה** — רק אל מאמן או אל שותף-קבוצה בפועל.
לפני כן כל משתמש מחובר יכול היה לפתוח שיחה עם כל קטין במערכת.

### קהילה
קהילת המאמנים (`community_posts`, `community_post_comments`, `community_post_likes`,
`community_messages`, `community_events`, `community_poll_votes`) — **קריאה וכתיבה למאמנים
בלבד** (`is_coach()`). מרחב השחקנים (`player_messages`) מוגבל לשותפי-קבוצה — לא עוד
חדר ארצי אחד של קטינים זרים. לאדמין יש מדיניות DELETE על כל טבלת תוכן, כדי שאפשר
יהיה להסיר תוכן פוגעני בפועל.

### אכיפת חסימה והמתנה להסכמה
`supabase_consent_enforcement.sql` מוסיף **מדיניות RESTRICTIVE אחת על INSERT**
בכל טבלת תוכן. מדיניות restrictive מתאחדת עם הקיימות ב-AND, כך שאף מדיניות קיימת לא
נחלשת; פשוט נוסף תנאי-על: *החשבון פעיל ולא חסום*. משמעות מעשית: משתמש שנחסם, או קטין
שההורה שלו ביטל את ההסכמה, לא כותב כלום — גם בקריאת PostgREST ישירה מחוץ לאפליקציה.
**שני חריגים מכוונים:** בקשת מחיקת חשבון תמיד עוברת, גם מחשבון חסום, ופנייה לאדמין
(סעיף 4ג) נמצאת מחוץ לשער — אחרת מי שתקוע לא יכול לבקש עזרה.

> משלים ולא מחליף: יש להפעיל גם `ban_duration` ב-Supabase Auth כדי לפסול את ה-JWT עצמו
> (ראו צ'קליסט בסעיף 11).

---

## 4. מודל ההסכמה

`supabase_parent_consent.sql` + `supabase_consent_manage_fix.sql`.
הכלל: **קטין אינו יכול לאשר את עצמו.**

* גיל מחושב בשרת (`minor_age()`) — מדויק מתאריך לידה, ושמרני כשיש רק שנת לידה.
* קטין ללא הסכמה מקבל `approval_status = 'pending_parent'`.
* **קישור חד-פעמי להורה:** הטוקן הגולמי לעולם לא נשמר — רק `sha256` שלו. מי שמחזיק
  בקישור מחזיק בסוד; דליפת המסד אינה מאפשרת להתחזות להורה. `initial` תקף 72 שעות
  וחד-פעמי; `manage` תקף שנה ורב-פעמי.
* **ארבע קטגוריות נפרדות:** `basic` (חובה), `media_team`, `media_public`, `marketing`.
* **`consents` היא append-only** — טריגר חוסם UPDATE ו-DELETE, גם לבעל הטבלה. אין
  Foreign Key בכוונה, כדי שמחיקת חשבון לא תפיל את יומן הראיות.
* לכל רשומה: מי אישר, מתי (שעון שרת), גרסת הנוסח **שנקראת מהמסד** (הלקוח לא יכול לזייף
  גרסה), IP ו-user-agent.
* `basic = revoked` משעה את החשבון ופותח אוטומטית בקשת מחיקה.
* מייל ההורה **ננעל** ברגע שנרשמה הכרעה ראשונה — כדי שקטין לא יסיט את הפיקוח לכתובת
  שבשליטתו. `request_manage_link()` מנפיק קישור ניהול חדש, ותמיד לאותו הורה נעול.
* בגיל 18 `confirm_adult()` רושם הסכמה עצמאית ומוחק את פרטי ההורה.

**גרסאות מסמכים:** `consent_documents.version` = `v1 · 3.8.2026`, זהה ל-`TERMS_VERSION`
ב-`src/constants.js` ול-`<meta name="document-version">` בשלושת המסמכים תחת `public/`.
שינוי נוסח מחייב עדכון של שלושתם יחד, אחרת אי אפשר לקשור הסכמה לנוסח שאושר.
**שינוי המספר מסמן הסכמות קיימות כ-`needs_reconsent`** — כלומר ההורים יתבקשו לאשר
מחדש. זו התנהגות רצויה כששינוי מהותי, ומטרד כששינוי טכני. לא לשנות בהיסח הדעת.

### 4א. מצב מוגבל — המאמן כשומר סף

ההחלטה המרכזית בגל הזה. קטין שההורה שלו **טרם ענה** אינו נחסם מהאפליקציה: הוא נכנס,
עורך פרופיל, ומצטרף לקבוצה עם קוד. מה שחסום לו במצב הזה: מדיה, קהילה, צ'אטים והודעות.
המאמן רואה במסך אישור הבקשות אם ההורה אישר, ומאשר בעיניים פקוחות — הוא הגורם האנושי
היחיד שמכיר את המשפחה בפועל.

`suspended` (הורה שביטל הסכמה) הוא סיפור אחר לגמרי ונשאר **נעול**.

* למה לא נעילה מלאה: קטין שנעול לחלוטין מוותר, או פותח חשבון חדש עם שנת לידה מזויפת.
  מצב מוגבל משאיר אותו במערכת, גלוי למאמן, בלי גישה לשום דבר שנוגע לילדים אחרים.
* **מה זה כן:** צמצום נזק + פיקוח אנושי. **מה זה לא:** תחליף להסכמה. ההסכמה עדיין
  נדרשת, וההגבלות נאכפות בשרת (סעיף 3).
* ⚠ המודל הזה — «המאמן מאשר במקום אימות טכני של ההורה» — הוא הכרעה שמשלבת שיקול משפטי.
  **טעון בדיקת עורך דין.**

### 4ב. מה שלא נסגר: אין אימות טכני שמי שאישר הוא ההורה

אין תשתית מייל בפרויקט, ולכן הקישור עובר דרך הקטין (וואטסאפ/העתקה). קטין נחוש יכול
לפתוח אותו בעצמו. זה מתועד בגילוי מלא גם ב-`public/privacy.html`.

מה שכן נסגר: אי אפשר להסיט את הקישור למייל אחר, אי אפשר לחדש הסכמה שההורה ביטל, ואי
אפשר לדרוס סירוב על מדיה ציבורית. הפרונט תומך כבר היום גם בצורת התשובה `sent_to`
(שליחה בצד שרת בלי שהטוקן חוזר ללקוח), כדי שהוספת ספק מייל תהיה החלפה בשרת בלבד.

### 4ג. ערוץ הפנייה לאדמין

`supabase_admin_requests.sql`. הנעילות בסעיף 4 נכונות — ובלעדיהן קטין היה מאשר לעצמו —
אבל הן משאירות משפחות אמיתיות תקועות: הורה שהחליף מייל, הורים פרודים שההסכמה נרשמה על
הלא-נכון, תאריך לידה שהוקלד שגוי.

* `admin_requests` — פנייה חופשית לאדמין, **במכוון מחוץ** לשער החשבון-הפעיל, עם הגבלת
  קצב של 5 פניות פתוחות למשתמש.
* `admin_requests_list()` / `admin_mark_request_done()` — לצד האדמין.
* `admin_set_guardian()` — החלפת ההורה הרשום. זו הפעולה המסוכנת ברשימה, ולכן היא
  לאדמין בלבד ונרשמת ביומן.

### 4ד. הצלבת גיל — מה זה מוכיח ומה לא

`supabase_age_crosscheck.sql`. יש שני מקורות בלתי-תלויים לגיל: מה שהשחקן הצהיר בפרופיל,
ומה שהמאמן הזין בסגל. השחקן לא ראה מה המאמן כתב, והמאמן לא ראה מה השחקן הצהיר.

**מה זה כן:** מאתר *אי-הסכמה* בין שני בני אדם, ומדגיש את המקרה שמשנה — צד אחד אומר
קטין, השני אומר בגיר. `admin_age_mismatch_summary()` מחזירה גם כמה שורות בכלל ניתנות
להשוואה, כדי ש"0 אי-התאמות" לא ייקרא כ"הכול תקין" כשהאמת היא "אף אחד לא מילא".

**מה זה לא, ואסור להציג אחרת:**
* **זה לא אימות זהות.** אין תעודה, אין מסמך, אין צד שלישי. השורה קובעת שיש מחלוקת, לא מי צודק.
* **קטין שאמר למאמן את אותו שקר לא ייתפס.** זו המגבלה המרכזית ואי אפשר לסגור אותה בקוד.
* **המאמן יכול להיות הטועה** — טעות הקלדה בשנה היא דבר שבשגרה. לכן הניסוח בממשק הוא
  «שני המקורות אינם תואמים», לעולם לא «השחקן שיקר». **הניסוח הזה הוא חלק מההגנה.**
* **כיסוי חלקי** — רק כששני הצדדים מילאו נתוני לידה.

---

## 5. מדיה — ומה שכבר לא נאסף

### 5א. לשחקנים אין תמונת פרופיל

`supabase_no_player_avatars.sql`. רוב השחקנים קטינים, ותמונת פנים של ילד היא הפריט
הרגיש ביותר במערכת. במקום לבנות סביבה שלוש שכבות הגנה — פשוט לא אוספים אותה.
**מה שלא קיים אי אפשר לדלוף, אי אפשר לדרוש במסגרת אכיפה, ואי אפשר לשכוח למחוק.**

* כתובות התמונה הקיימות של שחקנים אופסו.
* העלאה לתיקיית `avatars/` מחשבון שחקן **חסומה בשרת**, לא רק בטופס.
* הקבצים הישנים נמחקו מה-Storage בפועל.
* בממשק מוצגות ראשי תיבות של השם — התנהגות ברירת המחדל של `Avatar.jsx`.

⚠ **מה זה לא פותר:** תמונות שמאמן מעלה מהאימונים (`community/`, `drills/`) עדיין עלולות
להראות פנים של קטינים. עליהן ממשיכות לחול הסכמות המדיה (`media_team` / `media_public`)
ומדיניות ה-Storage. ההחלטה הסירה את התמונה שהילד מעלה על עצמו — לא כל תמונה שבה הוא מופיע.

### 5ב. ה-bucket

`supabase_private_media.sql`. לפני כן ה-bucket `media` היה **ציבורי ובלי ולו מדיניות
אחת** — קובץ ישב בכתובת קבועה, בלי אימות ובלי תפוגה, וגם אחרי מחיקת החשבון.

* קריאה מותרת רק לפי מדיניות `storage.objects`, והאפליקציה ניגשת ב-**Signed URL** לזמן מוגבל.
* כתיבה ומחיקה מוצמדות לתיקיית הבעלים (`<uid>/…`).
* חתימת URL עובדת גם על bucket ציבורי, ולכן סדר הפריסה הוא: מיגרציה → פריסת פרונט →
  **ורק אז** `update storage.buckets set public = false`. ההיפוך הפיך בשנייה.
* בחלק מפרויקטי Supabase יצירת מדיניות על `storage.objects` נכשלת מה-SQL Editor
  (`must be owner of table objects`). הקובץ מדפיס אז NOTICE עם ההגדרה המדויקת ליצירה
  ידנית — **אסור להריץ את ההיפוך עד שהמדיניות נוצרה**.
* סרטוני שחקנים מוצגים רק אחרי אישור מנהל.

---

## 6. יומן התיעוד (audit log) — והמגבלה המוצהרת שלו

`supabase_audit_retention.sql`. עד היום לא היה שום תיעוד: הלוגים המובנים של Supabase
נשמרים ימים בודדים, ואחריהם אין דרך לענות על "מי שינה את מה, מתי, ועל מי".

* `audit_log` מתעד שינוי בעמודות הרגישות בלבד ב-`profiles`, `team_players`, `coach_notes`.
* **שתי רמות, וזו החלטת מזעור:** `plain` — הערך הישן והחדש נרשמים (role, is_admin, banned,
  approval_status, birth_date — כי בלי הערך אין ערך לתיעוד). `redact` — נרשם רק *שהשדה
  השתנה* ואורכו (injury_note, notes, טלפונים). **היומן לא הופך לעותק שני של תיק הפציעות.**
* קריאה לאדמין בלבד. אין policy של insert/update/delete לאף לקוח. טריגר חוסם עריכת שורה קיימת.
* `subject` מאפשר לענות על השאלה שהורה באמת שואל: "מי נגע בנתונים של הילד שלי".
* כשל בתיעוד לא מפיל שמירת פרופיל — הוא נרשם כאזהרה ב-Postgres logs. **היומן הוא best-effort.**

> ### ⚠ המגבלה, במפורש: היומן מתעד **שינויים**, לא **קריאות**
> תקנות אבטחת המידע מדברות גם על תיעוד *גישה*. תיעוד כל קריאה היה מחייב חסימת `select`
> לגמרי וניתוב כל שליפה דרך RPC — שינוי ארכיטקטוני בכל מסך, ונפח יומן אדיר (טעינת מסך
> קבוצה אחת = עשרות שורות). התחליף החלקי הקיים: `supabase_privacy4.sql` שלל `select` ברמת
> הטבלה על `profiles` והשאיר whitelist עמודות, כך שהעמודות הרגישות נחשפות רק דרך RPC —
> ואת אלה כן יהיה אפשר לתעד בעתיד בנקודה אחת.
> **⇦ הפער הזה חייב לעלות מול עורך הדין/ממונה אבטחת המידע. אל תניח שהיומן לבדו מסמן
> את דרישת התיעוד כ"תקין".**

---

## 7. שמירה, מחיקה, ומה באמת רץ

זה הסעיף שבו הכי קל להצהיר על מנגנון שלא קיים, ולכן הוא כתוב בשלושה חלקים: מה מוגדר,
מה מוחק, ומי קורא לו.

### 7א. החלונות — `retention_policy`

טבלה אחת עם `key / days / method / what / note`: `client_errors`, `consent_requests`,
`notifications`, `audit_log`, `team_players`, `coach_notes`. שינוי חלון הוא `update` על
שורה — **לא מיגרציה חדשה** — כדי שתיקון של עורך דין יהיה שורת SQL אחת.

> ⚠ **המספרים הם מצייני מקום הנדסיים, לא תקופות שאושרו משפטית.** היחיד שיש לו עוגן הוא
> 30 הימים שמופיעים במדיניות הפרטיות עצמה. **עורך דין חייב לאשר את כולם** — ובמיוחד את
> חלון שמירת יומן התיעוד, שיש לגביו דרישות שמירה מינימליות אפשריות בכיוון ההפוך.

### 7ב. המחיקה — `purge_expired_data(p_dry_run)`

נקודת כניסה אחת. **ברירת המחדל היא הרצה יבשה** — בלי ארגומנט היא סופרת ולא מוחקת.
`admin_purge_report()` מציג את הספירה היבשה לצד החלון, השיטה וההסבר.

* `team_players` עוברת **אנונימיזציה ולא מחיקה** — נוכחות, משחקים וסטטיסטיקה תלויים בשורה
  עם `on delete cascade`, ומחיקה הייתה משמידה למאמן עונה שלמה. מאפסים את המידע האישי
  ומשאירים שלד בלי זהות.
* `coach_notes` נמחקות — טקסט חופשי על קטין שאי אפשר לאנונימז באמינות.
* `consents` **אינה נמחקת לעולם** — היא יומן הראיות. מה שנמחק הוא `consent_requests`, כלומר
  ההזמנות בלבד.
* `unlinked_at` הוא שעון העזיבה. **אין backfill בכוונה:** לשורות שקיימות מלפני המיגרציה
  הוא null, ולכן הן לא ייכנסו לאנונימיזציה אוטומטית — אי אפשר לדעת בדיעבד מי מהן "עזבה",
  וניחוש היה משמיד roster חי. הספירה מתחילה מהיום.

### 7ג. מי קורא לזה — `supabase_retention_schedule.sql`

הפער שהקובץ הזה סוגר: פונקציה שאיש לא קורא לה שווה, מבחינת נושא המידע, בדיוק כמו
פונקציה שלא נכתבה.

* הקובץ מנסה להפעיל `pg_cron` ולתזמן ריצה יומית. אם ההרחבה לא זמינה בפרויקט — **הוא לא
  נכשל**, אלא מדפיס בעברית מה להפעיל בדשבורד ואיזו שורה להדביק אחרי.
* `retention_schedule.armed` הוא המתג. **ברירת המחדל: לא דרוך** — הריצה המתוזמנת סופרת,
  כותבת את המספר ליומן, ולא מוחקת דבר. כך יש הוכחה שהמנגנון פועל **לפני** שנוגעים בנתונים.
* `CHECK` ברמת המסד: **אי אפשר לדרוך בלי למלא `lawyer_ok_at`.** זו לא בדיקה משפטית —
  המסד אינו יודע מי חתם. זו מכשלה מכוונת שמכריחה לעצור ולתעד שהשאלה נשאלה.
  **הדריכה עצמה טעונה אישור עורך דין וגיבוי לפניה.**
* `admin_retention_status()` מחזיר: האם יש משימה מתוזמנת, מתי רצה בפועל, כמה שורות,
  ומה החלונות. `state` יכול להיות `not_scheduled` / `never_ran` / `dry_only` / `running` / `stale`.
  **"יש לנו retention" הוא מעכשיו דבר שבודקים במסך, לא דבר שמניחים.**

### 7ד. מחיקת חשבון — מה קורה באמת

* המשתמש מגיש בקשה ממסך הפרופיל (`account_deletion_requests`), או במייל. הבקשה עוברת
  גם מחשבון חסום או מושעה.
* **הביצוע ידני:** האדמין מוחק את החשבון, שורות הסגל וקבצי המדיה, ורק אז מסמן טופל.
  התחייבות 30 יום. אין מחיקה אוטומטית — מתועד בגילוי מלא במדיניות הפרטיות.
* מה שהשתנה בגל הזה: קודם, מחיקת חשבון השאירה אצל המאמן שורת סגל **מלאה** (שם, טלפון,
  תאריך לידה, הערת פציעה) בלי שום חותמת זמן, לנצח. עכשיו הניתוק מסומן ב-`unlinked_at`
  והשורה נכנסת למסלול האנונימיזציה של 7ב.
* מה שנשאר גם אחרי מחיקה: **יומן ההסכמות**, כרשומת ראיה. היקף השמירה שלו **טעון בדיקת עורך דין**.

### 7ה. זכות עיון וייצוא

`supabase_my_data.sql`. עד לגל הזה מדיניות הפרטיות הבטיחה עיון בלי מימוש.

* `my_data_export()` — המשתמש על עצמו. `admin_data_export(p_user)` — לאדמין, כדי לענות
  לפנייה בכתב. `guardian_data_export(p_token)` — **להורה בלי חשבון**, מתוך קישור הניהול.
* שלושתן עוטפות בונה אחד משותף, כדי שלא ייווצר מסלול שמחזיר פחות ממה שהמסד מחזיק —
  "ייצוא חלקי" הוא בדיוק מה שנכשל בעין של רגולטור.
* פונקציות העזר של הייצוא הן **בכוונה `SECURITY INVOKER`** ולא DEFINER (ראו סעיף 12).

---

## 8. סיסמאות — מה באמת אוכף

**האכיפה היא בדשבורד. הקוד הוא נוחות.**

`src/Auth.jsx` חוסם הרשמה מתחת ל-8 תווים, אין בדיקת מורכבות ואין בדיקת סיסמאות שדלפו.
כל זה בקליינט בלבד: קריאה ישירה ל-`auth/v1/signup` עוקפת אותו לחלוטין ומקבלת את מה
שמוגדר ב-Supabase Auth (ברירת מחדל: 6 תווים).

לכן: מי שרוצה מינימום 8 תווים חייב להגדיר אותו **בדשבורד**. הצ'קליסט בסעיף 11 הוא
המקום שבו זה נאכף, ולא הקוד. הבדיקה בקליינט קיימת כדי שהמשתמש יקבל הודעה בעברית
לפני שהשרת דוחה אותו — לא כדי להגן.

---

## 9. שרת מול לקוח — מה נאכף איפה

| נאכף בשרת (אי אפשר לעקוף) | קיים רק בלקוח (נוחות, לא הגנה) |
|---|---|
| מי רואה איזה פרופיל (RLS) | הסתרת מסכים וטאבים לפי `role` |
| מי יכול לשלוח DM למי | חיווי "אין לך הרשאה" |
| קהילת מאמנים סגורה לשחקנים | ניווט נפרד לעולם השחקן |
| חסימה והשעיה חוסמות כתיבה | מסך `PendingApproval` והבאנר המוגבל |
| הגבלות המצב המוגבל (מדיה, קהילה, צ'אט) | הסתרת הכפתורים |
| רישום ההסכמה, גרסת הנוסח, ה-IP והזמן | הצ'קבוקסים בטופס |
| חישוב הגיל | שדה תאריך הלידה |
| נעילת `role` ונעילת מייל ההורה | בורר התפקיד בהרשמה |
| חסימת העלאת תמונת פרופיל לשחקן | הסרת שדה ההעלאה מהטופס |
| פענוח קוד הצטרפות ויצירת הבקשה (`join_with_code`) | תיבת הקוד |
| כתיבת יומן התיעוד (טריגר) | — |
| מי רשאי להריץ ניקוי (`is_admin` בגוף הפונקציה) | — |
| הרשאות Storage | דחיסת תמונה לפני העלאה |
| ולידציית אורך/תוכן ב-constraints | ולידציית טפסים |
| **אורך הסיסמה — רק אם הוגדר בדשבורד** | 8 התווים ב-`Auth.jsx` |

**כלל לכל שינוי עתידי:** אם ההגנה היחידה היא `if (!isCoach) return null` — היא לא הגנה.

---

## 10. סיכונים מקובלים (מודעים ומתועדים)

1. **מפתח ה-anon ציבורי בכוונה.** `VITE_SUPABASE_ANON_KEY` נארז בבאנדל, זה התכנון של
   Supabase. הוא חסר ערך בלי RLS תקין — ולכן RLS הוא קו ההגנה היחיד, וכל מיגרציה חדשה
   חייבת להיבדק מולו. **service-role key לעולם לא בקוד הלקוח.**
2. **כל משתנה `VITE_*` הוא ציבורי.** כולל `VITE_YOUTUBE_API_KEY`. ההגנה היחידה עליו היא
   הגבלת referrer + API בקונסולת Google ותקרת מכסה — ולא סודיות.
3. **`public/__devlogin.html` בהיסטוריית git.** הקובץ הכיל access/refresh token אמיתיים
   של חשבון המאמן. הקובץ הוסר, **הטוקן פג והסשן בוטל**, אך היסטוריית הקומיטים עדיין
   מכילה את המייל האישי, את ה-UUID ואת מבנה מפתח ה-localStorage. **החלטה: הסיכון מקובל**
   כל עוד הריפו נשאר פרטי ולא בוצע שכתוב היסטוריה. אם הריפו ייפתח לציבור — חובה
   `git filter-repo`/BFG + force-push לכל הענפים לפני כן.
4. **מפתח Google שהיה בקוד** (`src/constants.js` בקומיט הראשוני, הוסר מאז) נגיש בהיסטוריה.
   נדרשת רוטציה בקונסולת Google — ראו צ'קליסט.
5. **סשן ב-localStorage ללא תפוגת חוסר-פעילות.** גניבת סשן דורשת XSS; ה-CSP הקשוח
   (`script-src 'self'`, בלי `unsafe-inline`) והיעדר `dangerouslySetInnerHTML` מצמצמים
   זאת מאוד. סיכון שיורי מקובל; שיפור מתוכנן: ניתוק אוטומטי אחרי חוסר פעילות.
   **`flowType` הוא `implicit` בכוונה** (`src/supabaseClient.js`): ב-PKCE ה-`code_verifier`
   נשמר רק במכשיר שביקש את הקישור, ולכן קישור איפוס סיסמה שנפתח בטלפון במקום במחשב
   נכשל לגמרי. מעבר ל-PKCE מחייב קודם טיפול מפורש ב-`?code=` בכל נקודת נחיתה, או
   מעבר לתבנית `token_hash` + `verifyOtp` בתבניות המייל.
6. **מחיקת חשבון ידנית.** ראו 7ד. אין מחיקה אוטומטית, וההתחייבות היא 30 יום ואנושית.
7. **ייצוא CSV של סגל.** מאמן יכול להוריד פרטי קטינים למחשבו. מוצגת אזהרת אחריות,
   והעמודות הרגישות (`notes`, `injury_note`) אינן נכללות בקובץ — אך מרגע ההורדה הקובץ
   מחוץ לשליטתנו.
8. **מעבדי משנה בחו"ל.** Supabase, Vercel (+Analytics), YouTube ו-rss2json מקבלים IP
   ומטא-דאטה של כל מבקר, כולל קטינים. מפורט ב-`public/privacy.html` §3.
   **הגופנים כבר אינם ברשימה** — הם מתארחים אצלנו החל מהגל הזה, כלומר טעינת עמוד לא
   מדליפה עוד IP של קטין אל Google. הצמצום הבא שנשאר על השולחן: הסרת Vercel Analytics.
   ⚠ **העברת מידע של קטינים לחו"ל טעונה בדיקת עורך דין** (תיקון 13).
9. **תיעוד קריאות אינו קיים** — ראו המסגרת בסעיף 6. סיכון מקובל **בכפוף** לכך שעורך הדין
   או ממונה אבטחת המידע יקבע שרמת האבטחה של המאגר אינה מחייבת אחרת.
10. **אין אימות טכני של זהות ההורה** — ראו 4ב. הפיצוי הוא המאמן כשומר סף. **טעון בדיקת עורך דין.**
11. **הצלבת הגיל אינה אימות** — ראו 4ד. קטין שאמר את אותו דבר לשני המקורות אינו מתגלה.
12. **הניקוי אינו דרוך.** נכון להיום המנגנון רץ יבש בלבד (7ג), כלומר תקופות השמירה
    **עדיין לא נאכפות בפועל**. זה מכוון — אסור למחוק לפי מספרים שלא אושרו — אבל כל עוד
    זה המצב, ההבטחה במדיניות הפרטיות נשענת על המחיקה הידנית של סעיף 7ד בלבד.

---

## 11. צ'קליסט תפעולי לבעלים (Supabase Dashboard)

דברים שאי אפשר לקבע בקוד — יש לוודא ידנית, ולחזור על הבדיקה אחרי כל שינוי בפרויקט:

**Authentication → Policies / Providers** (ראו סעיף 8 — כאן זו האכיפה היחידה)

- [ ] **Minimum password length = 8** (Authentication → Sign In / Providers → Password).
      ⚠️ **לא להעלות מעל 8 בלי שינוי בקוד**: המספר 8 קשיח בשלושה מקומות ב-`src/Auth.jsx`
      (הוואלידציה, ה-placeholder, וההסבר מתחת לשדה), ו-`translateError` מתרגם כל שגיאת
      `Password should be at least N characters` להודעה הקבועה "הסיסמה חייבת להכיל לפחות
      8 תווים". אם יוגדר בדשבורד 10, משתמש שינסה 8 תווים ייכשל ויקבל הודעה שאומרת לו
      לעשות בדיוק את מה שכבר עשה — לולאה בלי מוצא. לשינוי המספר: לעדכן קודם את הקוד.
- [ ] **Password Requirements** (אותיות גדולות/קטנות + ספרות) — כרגע **לא מומלץ להפעיל**.
      אין לזה שום בדיקה מקבילה בקליינט ואין ענף תרגום ב-`translateError`, ולכן הודעת
      השגיאה של GoTrue תוצג באנגלית גולמית תחת "משהו השתבש: ". להפעלה — קודם להוסיף
      בדיקה בקליינט וענף תרגום.
- [ ] **Leaked password protection** (בדיקת HaveIBeenPwned) — מופעל. זו ההגנה היחידה
      ברשימה הזו שאין לה תלות בקוד: היא פועלת בשרת ומחזירה שגיאה שכבר נופלת לענף גנרי.
- [ ] **CAPTCHA** על הרשמה והתחברות — מופעל (מונע יצירת חשבונות בהמוניהם).
- [ ] **Rate limits** — הגבלת OTP/מיילים לשעה, והגבלת ניסיונות התחברות.
- [ ] `ban_duration` פעיל, כדי שסימון `banned` יפסול גם את ה-JWT הקיים ולא רק את הכתיבה.
- [ ] אימות מייל בהרשמה — מופעל.
- [ ] Site URL ו-Redirect URLs מוגבלים לדומיין הייצור בלבד.

**Database / API**
- [ ] RLS מופעל על **כל** טבלה חדשה. בדיקה: `select tablename, rowsecurity from pg_tables where schemaname='public' and not rowsecurity;` — חייב לחזור ריק.
- [ ] אחרי כל מיגרציה: `select * from public.schema_migrations order by ran_at;`
- [ ] מדיניות `USING(true)`: `select tablename, policyname from pg_policies where schemaname='public' and qual='true';`
      — **חייבת לחזור שורה אחת בלבד**, `consent_documents`, שההורה קורא בלי חשבון.
- [ ] אזור השרתים של הפרויקט מתועד (מדיניות הפרטיות מצהירה "מחוץ לישראל").
- [ ] גיבויים אוטומטיים פעילים, ונבדק ששחזור עובד.

**Retention (סעיף 7)**
- [ ] `pg_cron` מופעלת (Database → Extensions), והמשימה קיימת:
      `select jobname, schedule, active from cron.job;`
- [ ] `select public.admin_retention_status();` מחזיר `state` שאינו `not_scheduled`.
- [ ] הדריכה נעשית **רק** אחרי: אישור החלונות ע"י עורך דין → גיבוי → `admin_purge_report()`.

**Storage**
- [ ] ה-bucket `media` מוגדר **Private** אחרי פריסת ה-Signed URLs.
- [ ] קיימות מדיניות SELECT/INSERT/UPDATE/DELETE על `storage.objects` (לא רק בהערות בקובץ).
- [ ] אין קבצים שרדו תחת `avatars/` של חשבונות שחקן (סעיף 5א מוחק — כדאי לאמת בעין).

**מפתחות**
- [ ] מפתח ה-YouTube סובב אחרי שנחשף בהיסטוריית git.
- [ ] המפתח מוגבל ל-YouTube Data API v3 בלבד + HTTP referrer של דומיין הייצור + תקרת מכסה יומית.
- [ ] service-role key לא קיים בשום קובץ בריפו ובשום משתנה `VITE_*`.
- [ ] הריפו פרטי (ראו סיכון מקובל 3).

**תפעול שוטף**
- [ ] בקשות מחיקת חשבון נבדקות בפאנל האדמין ומסומנות רק אחרי מחיקה בפועל (חשבון + שורות סגל + קבצי מדיה).
- [ ] פניות `admin_requests` נבדקות — שם יושבות המשפחות התקועות.
- [ ] אי-התאמות גיל (`admin_age_mismatch_summary`) נסקרות מדי פעם, בניסוח של בדיקה ולא של האשמה.
- [ ] תלונות על התנהגות מול קטין → חסימה בפאנל **וגם** אימות שהמשתמש אכן נותק.

---

## 12. כללי אצבע למפתחים ולסוכנים

* כל שאילתה מפרקת `{ data, error }`. שגיאת הרשאה שנבלעת נראית בדיוק כמו "אין נתונים" — כך מסך אישור בקשות ההצטרפות היה ריק בשקט חודשים.
* כל מיגרציה אידמפוטנטית, והפרונט חייב לשרוד פרודקשן שטרם הריץ אותה: קוד שגיאה
  `42703 / 42883 / 42P01 / PGRST202 / PGRST205` = "עדיין לא נפרס", ונופלים להתנהגות הישנה
  (`isNotDeployed()` ו-`callRpc` ב-`src/consent.js`).
* **לא עורכים מיגרציה שכבר רצה בייצור.** תיקון = קובץ חדש.
* כל פונקציה חדשה: `SECURITY DEFINER` + `set search_path = public` + `revoke ... from public, anon`
  + grant מפורש. **חריג חשוב:** `supabase_hardening_medium_3_8.sql` מריץ לולאה שמעניקה
  EXECUTE ל-`authenticated` לכל פונקציית DEFINER ב-`public`. לכן פונקציית עזר שאסור
  שמשתמש יקרא לה חייבת להיות **`SECURITY INVOKER`** — ואם היא DEFINER, השער חייב לשבת
  **בגוף הפונקציה** (`if not is_admin() then raise`), לא בהרשאה.
* מדיניות חדשה נבדקת מול קריאת REST ישירה עם ה-anon key, לא רק דרך המסך.
* אין `dangerouslySetInnerHTML`. כל קישור חיצוני עובר `safeUrl()` (חוסם `javascript:`).
* אין להוסיף דומיין חיצוני בלי לעדכן את ה-CSP ב-`vercel.json` **ואת סעיף 3 במדיניות הפרטיות**.
* כל טענה חדשה במסמכים המשפטיים שאין לה מנגנון רץ מאחוריה — אל תכתוב אותה. הפער בין
  "הבטחנו" ל"קיים" הוא בדיוק מה שהגל הזה בא לסגור.
