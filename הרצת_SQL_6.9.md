# הרצת SQL — 6.9.2026 (תיקוני הפיילוט)

קובץ אחד. שתי דקות. הוא **לא יוצר טבלאות ולא מוחק שום מידע** — הוא מתקן
חמישה דברים שהתגלו בבדיקה לפני הפיילוט. בלעדיו האפליקציה ממשיכה לעבוד
בדיוק כמו אתמול, עם חמש הבעיות.

---

## מה זה מתקן — בשפה שלך

**1. ילד שדחית בטעות היה נעול לתמיד.**
ברשימת בקשות ההצטרפות, לחיצה על «דחה» (גם טעות מגע) סגרה את הדלת
לצמיתות: הילד יכול היה להקליד את הקוד שוב ושוב, המסך שלו אמר לו «הבקשה
נשלחה, ממתין לאישור המאמן» — ואצלך לא הופיע כלום. מהיום, ילד שנדחה
ומקליד שוב את הקוד חוזר לרשימת הממתינים שלך, ואתה מקבל התראה כרגיל.

**2. «המידע שלי» לא הראה את תשובות הבוקר.**
הכפתור שמייצא לילד (או להורה) «את כל המידע שמוחזק עליי» החזיר הכול —
חוץ מדיווחי הצ'ק-אין, שהם דווקא הדבר הרגיש ביותר שאנחנו שומרים (שינה,
אנרגיה, כאב, «אני חולה»). הקובץ נקרא «כל המידע», אז עכשיו הוא באמת כולל
גם אותם.

**3. אפשר היה למחוק דגל אדום בלי שתדע.**
הגנו על הסימונים שלך («ראיתי» / «דיברתי איתו») אבל שכחנו את המחיקה:
ילד (או חבר שמשחק עם הטלפון שלו) יכול היה למחוק את הדיווח עצמו, ואצלך
היום היה נראה נקי — בלי שורה, בלי סימן, בלי שתדע שדווח כאב. מהיום
המחיקה הישירה חסומה. הזכות של הילד למחוק את התשובות שלו נשמרת, אבל היא
משאירה **שורה אחת ביומן**: כמה דיווחים נמחקו, בין אילו תאריכים, וכמה
מהם היו מסומנים — בלי התשובות עצמן.

**4. עכשיו יש תשובה להורה שאומר «ותמחקו גם את מה שכבר יש».**
המתג «בלי שאלות בוקר» עצר רק דיווחים חדשים; מה שכבר נאסף נשאר. נוספה
פקודה שמוחקת את כל הדיווחים של ילד אחד אצלך (ראו «מחיקה לילד אחד»
למטה). אין לה עדיין כפתור במסך — בינתיים זו שורה שמריצים ב-SQL Editor.

**5. «נשמר 90 יום» לא היה מחובר לכלום.**
הכתוב במסמכים אמר שהדיווחים נמחקים אחרי 90 יום, אבל שום דבר לא הריץ את
המחיקה. עכשיו הצ'ק-אין מחובר לאותו מנגנון ניקוי כמו כל השאר.
**⚠ שים לב — זה עדיין לא מוחק לבד:** כל מנגנון הניקוי במערכת מוגדר
במכוון כ«יבש» (סופר ולא מוחק) עד שעורך דין יאשר את החלונות. מה שהשתנה:
הצ'ק-אין כבר לא נשכח מהספירה, וברגע שתדרכו את המנגנון — הוא ינוקה יחד
עם כולם. עד אז, המחיקה בפועל היא שורה ידנית פעם בחודש (למטה).

---

## הקובץ: `supabase_pilot_fixes_6_9.sql`

### סדר

**אחרי #46** (`supabase_checkins_4_9.sql`, שכבר רץ). לא תלוי ב-#47
(הקובץ המשפטי) — אפשר להריץ לפניו או אחריו.

⚠ **דבר אחד לזכור לתמיד:** הקובץ הזה מחליף ארבע פונקציות שנוצרו בקבצים
קודמים. אם אי פעם תריץ מחדש את `supabase_hardening_medium_3_8.sql`, את
`supabase_my_data.sql`, את `supabase_checkins_4_9.sql` או את
`supabase_retention_schedule.sql` — הרץ את הקובץ הזה **שוב אחריהם**,
אחרת התיקונים נדרסים בשקט.

### איך מריצים

Supabase → SQL Editor → New query → מדביקים את כל תוכן הקובץ → Run.
בטוח להרצה חוזרת (אפשר להריץ פעמיים, לא קורה כלום).

### לבדוק שעבד (שלוש שאילתות)

```sql
-- 1) הבקשה שנדחתה נפתחת מחדש — צריך להחזיר true
select prosrc like '%reopened_after_reject%' as ok
  from pg_proc where proname = 'join_with_code';

-- 2) «המידע שלי» כולל צ'ק-אין — צריך להחזיר true
select prosrc like '%player_checkins%' as ok
  from pg_proc where proname = '_data_export_for';

-- 3) הדגל האדום מוגן: אין יותר מחיקה ישירה, והמדיניות פוצלה לשלוש.
--    השורה הראשונה צריכה להחזיר false; השנייה — pc_player_read /
--    pc_player_insert / pc_player_update (ובלי pc_player_own)
select has_table_privilege('authenticated', 'public.player_checkins', 'delete')
       as delete_should_be_false;
select policyname, cmd from pg_policies
 where tablename = 'player_checkins' order by policyname;
```

בונוס (לא חובה) — לראות שהצ'ק-אין נכנס לספירת הניקוי. זו **הרצה יבשה**:
היא רק סופרת, לא מוחקת.

```sql
select public.retention_run_scheduled(true);
-- בתוך rows צריך להופיע המפתח player_checkins
```

ובאפליקציה: אם יש ילד שדחית בטעות — בקש ממנו להקליד שוב את קוד הקבוצה.
הוא אמור לחזור אליך לרשימת «ממתינים לאישור».

### ניקוי 90 יום (פעם בחודש, ידני)

זה לא השתנה — עדיין ידני, כי המחיקה מחכה לאישור עורך דין:

```sql
select public.checkins_purge();       -- יבש: כמה שורות היו נמחקות
select public.checkins_purge(false);  -- מחיקה בפועל (רק אחרי שהיבש נראה הגיוני)
```

**קבע לעצמך תזכורת חוזרת בטלפון, 1 בחודש: «להריץ את שתי השורות האלה».**
כל עוד אין תזכורת — ההבטחה «90 יום» היא הבטחה שאף אחד לא מבצע.

### מחיקה לילד אחד (ההורה ביקש)

צריך את מזהה שורת הסגל של הילד. שתי שורות, לפי הסדר:

```sql
-- א) למצוא את המזהה (החלף את השם; ב-SQL Editor אין «מי מחובר», לכן
--    מחפשים לפי שם וקבוצה)
select id, name, team from public.team_players
 where name ilike '%יובל%' order by team;

-- ב) למחוק את כל דיווחי הצ'ק-אין שלו (החזרה: {"ok":true,"deleted":N})
select public.purge_checkins_for_roster('<המזהה מהשורה הקודמת>');
```

מה-SQL Editor הפקודה עובדת (אין שם משתמש מחובר, וזה בסדר — היא מוחקת
רק אצל המאמן שרשום על שורת הסגל עצמה). מתוך האפליקציה, אם יום אחד
יתווסף כפתור, היא תעבוד רק אצל המאמן שהילד שלו.

⚠ פעולה בלי חזרה. היא מוחקת רק דיווחי צ'ק-אין, ורק את מה שנאסף אצלך.
כדאי גם להדליק לילד את המתג «בלי שאלות בוקר» בפרטי השחקן, אחרת הוא
ידווח שוב מחר.

### ביטול

הבלוק המלא נמצא בסוף קובץ ה-SQL עצמו («ביטול (rollback)»). הקיצור:

```sql
-- להחזיר לילד את זכות המחיקה הישירה ואת המדיניות המאוחדת:
drop policy if exists pc_player_read   on public.player_checkins;
drop policy if exists pc_player_insert on public.player_checkins;
drop policy if exists pc_player_update on public.player_checkins;
grant delete on public.player_checkins to authenticated;
-- (ואז להריץ מחדש את supabase_checkins_4_9.sql — הוא מחזיר את pc_player_own)

-- להסיר את כלי המחיקה החדש (אין לו כפתור, זה לא משפיע על שום מסך):
drop function if exists public.purge_checkins_for_roster(uuid);

-- שאר הפונקציות: להריץ מחדש את הקובץ המקורי של כל אחת —
--   supabase_hardening_medium_3_8.sql  (בקשות הצטרפות)
--   supabase_my_data.sql               («המידע שלי»)
--   supabase_retention_schedule.sql    (הניקוי המתוזמן)

notify pgrst, 'reload schema';
```
