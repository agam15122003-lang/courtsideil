# הרצת SQL — 19.8.2026

קובץ אחד. שתי דקות. אפשר גם לא להריץ עכשיו — כל השאר עובד בלעדיו.

---

## מה כבר נעשה אתמול בערב (לא צריך לגעת)

| קובץ | מצב |
|---|---|
| `supabase_game_quiz_hardening_13_8.sql` | ✅ **רץ ואומת** — שלוש העמודות קיימות וארבע הפונקציות המוקשחות בשרת. חור הזיוף בטבלת הנקודות סגור |
| `supabase_notebook_18_8.sql` | ✅ רץ 18.8 |
| `supabase_dossier_18_8.sql` | ✅ רץ 18.8 |

---

## הקובץ להרצה: `supabase_club_manage_19_8.sql`

**למה הוא צריך להיות:** מ-18.8 מנהל מועדון כבר רשאי לראות את **התיקים** של
מאמנים שצורפו לעץ. אבל למסך אין דרך להגיע אליהם: המסך הולך קבוצה → שחקן →
תיק, ועל טבלת הסגל אין הרשאה שמתירה למנהל לראות את הסגל של מאמן אחר.
הקובץ מוסיף בדיוק את ההרשאה הזו, ורק אותה.

**מה הוא עושה:**
1. פונקציה `club_manager_sees_coach` — «האם אני מנהל של המועדון שהמאמן הזה
   צורף אליו».
2. הרשאת קריאה על טבלת הסגל למנהל — **קריאה בלבד**.

**מה הוא לא עושה:** לא נותן למנהל לשנות דירוג, מדידה או רשומה. הדירוג נשאר
של מי שמאמן. לא נוגע בשחקנים ובהורים — הם לא רואים תיקים בכלל.

### איך מריצים

Supabase → SQL Editor → New query → מדביקים את כל תוכן הקובץ → Run.
בטוח להרצה חוזרת.

### לבדוק שעבד

```sql
select policyname from pg_policies
 where tablename = 'team_players' and policyname = 'roster_club_manager_read';
```

צריכה לחזור **שורה אחת**. אם חזר ריק — הקובץ לא רץ.

### ביטול

```sql
drop policy if exists roster_club_manager_read on public.team_players;
```

המסך של המנהל יחזור להיות ריק. שום נתון לא נמחק ושום דבר אחר לא נפגע.

---

## ואחרי זה — שורה אחת שרק אתה יכול (אם רצית שהתפקיד יתפקד)

מינוי מנהל מועדון נעשה בידי מנהל המערכת בלבד, ובכוונה. בלי זה טאב
«המועדון» ו«הקטלוג» פשוט לא מופיעים — וזה מצב תקין, לא תקלה.

```sql
insert into public.club_roles (club, user_id, role, approved_by)
select p.club, p.id, 'club_manager', p.id
  from public.profiles p
 where p.id = (select id from auth.users where email = 'המייל-של-המנהל')
on conflict do nothing;
```

מרגע זה: **צירוף מאמנים לעץ ומינוי מנהל מקצועי כבר לא דורשים SQL** — הם
בטאב «המועדון» במסך תיק שחקן.

**ביטול המינוי:**

```sql
delete from public.club_roles where role = 'club_manager' and club = 'שם-המועדון';
```
