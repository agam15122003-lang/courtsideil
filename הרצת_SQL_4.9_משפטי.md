# הרצת SQL — הנוסחים המשפטיים (v3) · קובץ `supabase_legal_v3_4_9.sql`

קובץ אחד. דקה. הוא לא יוצר טבלאות, לא מוחק כלום, ולא נוגע באף חשבון —
הוא מעדכן שלוש שורות טקסט במסד.

> **6.9.2026 — מספר הגרסה עלה.** הקובץ נכתב ב-4.9 ולא רץ מעולם. ב-6.9
> תוקנו בו ובדפים הציבוריים שני דברים מהותיים (ביטול «אני חולה היום»
> מהמסך, ותשובות הצ'ק-אין שנכנסו לייצוא «המידע שלי»), ולכן מזהי הגרסה
> הם `v3 · 6.9.2026` ו-`v2 · 6.9.2026`. שם הקובץ לא השתנה.

---

## למה זה נדרש

שני הדפים הציבוריים (`privacy.html` ו-`terms.html`) היו בגרסה
`v2 · 24.8.2026`, ובה כתוב במפורש שאין חשבונות שחקן, אין קטינים רשומים
ואין מנגנון הסכמת הורים. מאז 4.9 כל השלושה **לא נכונים**: צד השחקן חזר
לפיילוט, נרשמים ילדים בני 15–18, ויש זרימת הסכמת הורה מלאה.

בנוסף, המסמך שההורה באמת קורא בטופס ההסכמה (`doc_a`) לא הזכיר כלל את
**הצ'ק-אין היומי** — שאלות הבוקר על שינה, אנרגיה, כאב ומחלה. זה איסוף
חדש על מצב הגוף של ילד, והוא חייב להופיע במסמך שההורה מאשר.

**מה כבר עודכן בקוד (בלי SQL):**

| מקום | לפני | אחרי |
|---|---|---|
| `public/privacy.html` | `v2 · 24.8.2026` | `v3 · 6.9.2026` |
| `public/terms.html` | `v2 · 24.8.2026` | `v3 · 6.9.2026` |
| `src/constants.js` — `TERMS_VERSION` | `v2 · 24.8.2026` | `v3 · 6.9.2026` |

> ⚠ **מקום רביעי — אפליקציית האנדרואיד.** היא אורזת עותק משלה של
> `privacy.html` / `terms.html` ואת `TERMS_VERSION` מהודר בתוך ה-APK,
> וכרגע שלושתם עדיין `v2 · 24.8.2026` — כלומר בדיוק הנוסח השקרי.
> **אחרי הפריסה חובה:** `npm run build && npx cap sync android`, לבנות
> APK חדש ולהפיץ אותו. עד אז: (א) מי שנרשם מתוך האפליקציה ייחתם על
> `v2 · 24.8.2026` בעוד המסד אומר `v3` — בדיוק אי-ההתאמה שהקובץ הזה בא
> לתקן; (ב) מי שיפתח «תנאי שימוש» מתוך האפליקציה יראה את הנוסח הישן.
> אם אי אפשר לבנות עכשיו — אפשר לא להריץ, או להריץ ולדעת שהרשמות
> מהאפליקציה בין לבין ייחתמו על `v2`.

**מה הקובץ הזה מעדכן במסד:**

| שורה ב-`consent_documents` | לפני (במסד) | אחרי |
|---|---|---|
| `terms` | `v1 · 3.8.2026` | `v3 · 6.9.2026` |
| `privacy` | `v1 · 3.8.2026` | `v3 · 6.9.2026` |
| `doc_a` | `v1 · 3.8.2026` | `v2 · 6.9.2026` + נוסח חדש |

> ⚠ שים לב לעמודה «לפני»: שורות `terms` ו-`privacy` במסד **מעולם לא
> עודכנו** מאז 3.8 — אף מיגרציה לא נגעה בהן. כלומר הן היו לא מסונכרנות
> עם הדפים כבר בגל של 24.8. הקובץ הזה מיישר את שלושת המקומות בבת אחת.
> לפני ההרצה כדאי להריץ את שאילתה 0 למטה ולראות מה יש שם באמת.

---

## הקובץ: `supabase_legal_v3_4_9.sql`

### סדר

אחרי `supabase_parent_consent.sql` (רץ מזמן). אין תלות בקבצים של 4.9 —
אפשר להריץ אותו לפני או אחרי `supabase_checkins_4_9.sql`.

### איך מריצים

Supabase → SQL Editor → New query → מדביקים את כל תוכן הקובץ → Run.
בטוח להרצה חוזרת. בסוף ההרצה יופיעו ב-Notices שלוש שורות עם הגרסאות
החדשות.

---

## מה זה עושה לחשבונות קיימים — קרא לפני שאתה לוחץ Run

### מה **לא** קורה (וזה החלק החשוב)

- **אף חשבון לא מושעה.** יומן ההסכמות (`consents`) הוא ראיה היסטורית
  ואינו ניתן לשינוי; הבדיקה «יש הסכמה?» לא מסתכלת בכלל על מספר הגרסה.
- **אף ילד שאושר לא חוזר ל«ממתין להורה».**
- **אף הורה לא נדרש לאשר מחדש** בכוח. שום דבר לא נחסם.
- **קישורי הסכמה שכבר שלחת להורים ממשיכים לעבוד.** ההורה יראה בהם את
  הנוסח **החדש** (הטופס קורא את המסמך בזמן אמת), וזה בדיוק מה שרצוי.

### מה כן קורה — שלוש תופעות לוואי מכוונות

1. **באנר רך «כדאי לאשר מחדש»** יופיע במסך של כל שחקן קטין שההסכמה שלו
   נרשמה על הנוסח הישן. זה באנר בלבד — הוא לא חוסם כלום.

2. ⚠ **המלכודת האמיתית — אל תשלח קישור ראשוני חדש.** אחרי העדכון,
   הפונקציה שיוצרת בקשת הסכמה תפסיק לומר «כבר אושר» ותנפיק **קישור
   ראשוני** חדש לילד. בקישור ראשוני הטופס נפתח **בלי סימונים**, וכל תיבה
   שההורה לא סימן נרשמת כ«סירוב» — כלומר הסכמות רשות שההורה כבר נתן
   בעבר (תמונות בקבוצה, למשל) עלולות לרדת בשקט.
   **המסלול הנכון לחידוש הוא «קישור הניהול»** — שם התיבות נפתחות עם מה
   שההורה כבר אישר. אם הורה רוצה לעבור על הנוסח החדש: תן לו קישור ניהול,
   לא בקשה חדשה.

3. **פרסום סרטונים ב«עולם הכדורסל» נסגר לכל הקטינים הקיימים** (הבדיקה
   דורשת התאמה מדויקת לגרסת `doc_a`). **לא מזיק היום** — «עולם הכדורסל»
   מוסתר (`BASKETBALL_WORLD = false`) וההגשות חסומות בשרת ממילא. חשוב
   לזכור את זה ביום שמדליקים אותו חזרה.

---

## לבדוק שעבד

```sql
-- 0) לפני ההרצה: מה יש שם עכשיו (3 שורות)
select id, version, updated_at from public.consent_documents order by id;

-- 1) אחרי ההרצה: הגרסאות החדשות (3 שורות)
--    צפוי: doc_a = v2 · 6.9.2026 · privacy = v3 · 6.9.2026 · terms = v3 · 6.9.2026
select id, version, updated_at from public.consent_documents order by id;

-- 2) הנוסח החדש של doc_a באמת מזכיר את הצ'ק-אין (צפוי: true, true, true)
select
  body_html like '%הצ''ק-אין היומי%'  as has_checkin_section,
  body_html like '%בלי שאלות בוקר%'   as has_optout,
  body_html like '%90 יום%'            as has_retention
from public.consent_documents where id = 'doc_a';

-- 3) המיגרציה נרשמה (שורה אחת)
select * from public.schema_migrations where filename = 'supabase_legal_v3_4_9.sql';

-- 4) שפיות: יומן ההסכמות לא נגעו בו — הספירה זהה לפני ואחרי
select count(*) as total_consents from public.consents;

-- 5) שפיות: אף אחד לא הושעה בגלל העדכון
select approval_status, count(*)
  from public.profiles where role = 'player'
 group by approval_status;

-- 6) מי יראה את הבאנר «לאשר מחדש» (קטינים על הנוסח הישן)
select p.id, p.first_name, c.doc_version, c.created_at
  from public.consents c
  join public.profiles p on p.id = c.minor_id
 where c.consent_type = 'basic' and c.value = 'granted'
   and c.doc_version <> 'v2 · 6.9.2026'
 order by c.created_at desc;
```

**ואחרי הפריסה, בדפדפן:** לפתוח `‎/privacy.html` ו-`‎/terms.html` ולוודא
ששניהם מציגים `v3 · 6.9.2026` בשורה העליונה. אם מופיע `v2` או `4.9` —
זה עותק שמור בדפדפן; רענון קשיח (Ctrl+F5).

---

## ביטול

הנוסח הישן של `doc_a` **אינו** שמור בקובץ הזה. כדי לחזור אחורה במלואו
צריך להעתיק את הבלוק המקורי מתוך `supabase_parent_consent.sql` (הבלוק
שמתחיל ב-`insert into public.consent_documents ... 'doc_a'`) ולהריץ אותו
עם `on conflict (id) do update` במקום `do nothing`.

החזרת מספרי הגרסה בלבד:

```sql
-- שים לב: זהו הערך שהיה **במסד** בפועל, ולא v2 שהופיע בדפים.
-- אם שאילתה 0 החזירה ערך אחר — החזר את מה שהיא הראתה, לא את מה שכתוב כאן.
update public.consent_documents set version = 'v1 · 3.8.2026' where id in ('terms','privacy');
notify pgrst, 'reload schema';
```

⚠ **אין** להחזיר את מספר הגרסה של `doc_a` בלי להחזיר גם את `body_html`.
גרסה ישנה עם נוסח חדש פירושה שכל הסכמה שתיחתם אחר כך תישא מספר גרסה
שאינו מתאר את הטקסט שההורה ראה — בדיוק שבירת הראיה שהקובץ הזה בא למנוע.
לכן אין כאן קיצור דרך «מספרים בלבד» ל-`doc_a`: שחזור שלו הוא הבלוק המלא
שמתואר למעלה.

ובמקביל להחזיר אחורה את `TERMS_VERSION` ב-`src/constants.js` ואת מזהי
הגרסה בשני קובצי ה-HTML. **חשוב:** רשומות הסכמה שכבר נחתמו על `v3`
יישארו עם `v3` — הן ראיה ואי אפשר לשנות אותן. זו סיבה טובה להריץ פעם
אחת ולא לשחק עם הגרסאות הלוך ושוב.

---

## מה עדיין פתוח (לא בקובץ הזה)

- **שאלות הבוקר יושבות בתוך ההסכמה הבסיסית ואין להן תיבה נפרדת בטופס.**
  זו הנקודה המשפטית החלשה ביותר ב-v3, והיא **השאלה הראשונה לעורך הדין.**
  להורה אין דרך לאשר את החשבון ולסרב לשאלות הגוף באותו מסך; המוצא היחיד
  הוא בקשה למאמן. תיבה חמישית דורשת מיגרציה (`consents.consent_type`
  מוגבל בבדיקת CHECK לארבעה ערכים, `supabase_parent_consent.sql:231`)
  ושינוי ב-`src/consent.js:288`. בינתיים נכתב במפורש בשלושת המסמכים
  שאפשר לבקש כיבוי כבר ברגע מתן ההסכמה, ושהמאמן מחויב להיענות.
- **`accessibility.html` נשאר `v2 · 24.8.2026`.** ההערה ב-`constants.js`
  תוקנה כך שהאינווריאנטה מכסה את `privacy.html` ו-`terms.html` בלבד, ושם
  מצוין ש-`accessibility.html` מנוהל בגרסה נפרדת — תוכנו לא השתנה ב-v3.
- **ההורה אינו יכול להפיק את קובץ הייצוא בעצמו.** `guardian_data_export`
  קיימת במסד, אבל `src/consent.js:416` מגדיר עוטף ש**אף מסך לא מייבא**,
  ובמצב `manage` הטופס מציג רק את תיבות ההסכמה. שלושת המסמכים תוקנו
  ואומרים זאת: בקשת עותק נעשית בפנייה למייל והיא מבוצעת ידנית, בתוך 30
  יום. אם יתווסף כפתור — לחזור ולחדד את הנוסח.
- **גם למאמן אין כפתור ייצוא.** `myDataExport()` נקראת רק מ-
  `PlayerDashboard`. מדיניות הפרטיות אומרת זאת עכשיו במפורש.
- ~~**הצ'ק-אין חסר בייצוא «המידע שלי».**~~ **נסגר ב-6.9** —
  `supabase_pilot_fixes_6_9.sql` מחליף את `_data_export_for` ומוסיף לה את
  המדורים `checkins` ו-`checkins_coach_logged`, וגם מזכיר את הקטגוריה
  בטקסט ההסבר שבתוך הקובץ המיוצא. שלושת המסמכים עודכנו בהתאם ואומרים
  עכשיו שהתשובות **כן** נכללות. ⚠ שני הקבצים נמסרים לבעלים באותה הרצה —
  אם רק הקובץ המשפטי ירוץ, המסמכים יבטיחו ייצוא שעדיין אינו כולל אותן.
- **אין כפתור «מחק את התשובות שלי», והמאמן אינו יכול למחוק במקומו.**
  `pc_coach_write` מוגבלת ל-`source = 'coach'`, ולכן למאמן אין זכות מחיקה
  על שורה שהשחקן כתב; `delete_my_checkins()` מוחקת רק `player_id =
  auth.uid()`. בפיילוט הנוכחי המאמן הוא גם מפעיל המסד ולכן זה לא נצרב,
  אבל ברגע שיצטרף מאמן שני — «תבקש מהמאמן» היה הופך למסלול מת. לכן
  שלושת המסמכים מפנים עכשיו את בקשת המחיקה **לכתובת המייל**, ומציינים
  שאפשר לבקש מהמאמן להעביר אותה לשם.
- **אין מסך היסטוריה בצד השחקן.**
  הפונקציה `delete_my_checkins()` קיימת במסד ומורשית, אבל **שום מסך
  בקוד לא קורא לה** (בדוק: `grep -rn delete_my_checkins src/` מחזיר
  ריק), והכרטיס מציג רק את התשובה של היום. לכן שלושת המסמכים מנוסחים
  ככה: «אפשר לבקש, והבקשה מבוצעת (כרגע ידנית)». **אם מוסיפים כפתור —
  אפשר וכדאי לחזור ולחדד את הנוסח.** עד אז, בקשת מחיקה או עיון של הורה
  היא משהו שאתה מבצע ידנית.
- **המחיקה אחרי 90 יום אינה אוטומטית.** המסמכים אומרים את זה בכנות.
  בפועל צריך להריץ `select public.checkins_purge(false);` פעם בחודש —
  ראו `הרצת_SQL_4.9.md`.
