# CLAUDE.md — CourtSide

אפליקציית כדורסל בעברית (RTL): React + Vite + Supabase, עולם מאמן + עולם שחקן. תיעוד מלא: `HANDOFF.md` (ארכיטקטורה), `README.md` (הקמה + סדר הרצת SQL), `DESIGN.md` (שפה עיצובית), `DESIGN_RESEARCH.md` (רקע).

## פקודות

- `npm run dev` — שרת פיתוח (5173; לבעלים יש שרת נפרד על 5174 — צריך hard refresh שם)
- `npm run verify` — **חובה אחרי כל שינוי**: check-imports (התנגשויות רישיות/ייבוא עמום) + build
- אין טסטים אוטומטיים; אימות = build + בדיקה חיה בדפדפן

## מוסכמות קריטיות

- **CSS**: קובץ אחד `src/index.css`, append-only — בלוק חדש עם banner מתוארך **בסוף הקובץ**. לא עורכים בלוקים ישנים.
- **SQL**: קובץ `supabase_*.sql` אידמפוטנטי חדש לכל מיגרציה + `mark_migration('<file>.sql')` עטוף exception + `notify pgrst, 'reload schema'` + שורה בטבלת README. הקוד חייב לשרוד פרוד שטרם הריץ את המיגרציה (fallback select / retry בלי העמודה).
- **i18n**: מחרוזות במסכים עם `L('עברית', 'English')` מ-`./i18n`. ה-DB שומר עברית.
- **שמות קבצים**: אסור שני קבצים שנבדלים רק ברישיות (Windows vs Linux — היה white-screen). `npm run check` תופס.
- כותרות commit בעברית קצרה; לא לגעת ב-`main` בלי לוודא build.

## עקרונות עיצוב

**המסמך המחייב: [DESIGN.md](DESIGN.md)** — פלטת "פרקט ולילה", טיפוגרפיה (Rubik / Heebo בלבד — Karantina הוסר ב-26.7 ו-`index.html` כבר לא טוען אותו; אין להחזירו), אנימציה, RTL, עשה/אל-תעשה. החוקים הקריטיים:

1. **RTL מלא** — הכיוון הראשי תמיד עברית-RTL. מספרים/שעונים/`X/Y` תמיד עם `dir="ltr"` או `<bdi>`; חץ "קדימה" הוא ← (ArrowLeft); מרווחים לוגיים (`margin-inline-*`), לא left/right.
2. **אייקונים רק מ-lucide-react** — אף פעם אימוג'י או SVG חיצוני כאייקון UI (אימוג'י מותר בתוך מחרוזות תוכן בעולם השחקן). כפתור-אייקון בלי טקסט חייב `aria-label`.
3. **בלי גרדיאנטים גנריים** — גרדיאנט-מבטא אחד למסך לכל היותר, תמיד מטוקנים של המותג (לילה/כתום/ירוק-התקדמות). אין hex גולמי ברכיבים — טוקנים בלבד.
4. **לכל מסך שלושה מצבים מעוצבים** — טעינה (`.loader`/skeleton), ריק (`.empty-state` עם אייקון + "מה עושים עכשיו"), ושגיאה (הודעה + התאוששות; שגיאת שאילתא אינה empty-state).

וגם: כותרת display אחת למסך; CTA ראשי אחד; יעדי מגע ≥44px; כל אנימציה חדשה כבויה תחת `prefers-reduced-motion` **וגם** `html.a11y-motion`; ניגודיות AA — כתום לטקסט על בהיר הוא `--accent-strong` בלבד, לעולם לא `--accent`.
