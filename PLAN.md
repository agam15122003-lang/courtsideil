# PLAN.md — תוכנית אנימציה ל-CourtSide

> קובץ מתגלגל: כל תשובה של Claude נכתבת גם לכאן. עודכן 28.7.2026, ענף `redesign`.

---

## החלטות שאושרו (28.7.2026)

| # | נושא | החלטה |
|---|---|---|
| 1 | סדר עבודה | **שלב א' בלבד — CSS בסיסי.** שלבים ב'/ג' אחר כך. |
| 2 | Lenis | רק בדפים הציבוריים (Landing / PublicDrill). **לא** בתוך האפליקציה. |
| 3 | ספירת מספרים בטיילים | **לא.** מאמן צריך לקרוא מספר מיד. ירד מהתוכנית לצמיתות. |

---

## חלוקת תפקידים בין הספריות

| ספרייה | תפקיד יחיד | אסור לה |
|---|---|---|
| **CSS (קיים)** | ברירת מחדל: hover, כניסות מסך, stagger, פסי התקדמות, skeletons | — |
| **motion** | מודלים / bottom-sheets / drawer / רשימות עם `layout` | **לא** כניסות מסך (DESIGN.md §3 — `AnimatePresence mode="wait"` נתקע בטאבים ברקע) |
| **gsap** | ציר-זמן מתוזמן ו-SVG | **לא** hover, לא כניסות כרטיסים |
| **lenis** | גלילה חלקה בדפים הציבוריים בלבד | **לא** ב-Dashboard / עולם השחקן |

נכון להיום אף אחת משלוש הספריות לא מיובאת בקוד. `package.json` כולל `motion@12.42.2`, `gsap@3.15.0`, `lenis@1.3.25`.

---

## ממצאי בדיקה — שלב א' המקורי התייתר

בבדיקת הקוד לפני הכתיבה התברר ששני הסעיפים של שלב א' לא רלוונטיים:

### א1 — "TacticsBoard: השמע מהלך" → **כבר בנוי, בלי GSAP**
`src/TacticsBoard.jsx:531-555` מכיל מנוע `requestAnimationFrame` מלא: אינטרפולציה בין שלבים, `easeInOutCubic` (שורה 558), play/pause, לולאה, ו-`autoPlay`.
חוזה הנגישות כבר מקוים ב-`TacticsBoard.jsx:404-417` — `autoPlay` מדלג גם על `prefers-reduced-motion` וגם על `html.a11y-motion`.
**מסקנה:** להחליף מנוע עובד ב-GSAP זה שכתוב, לא שדרוג. הסעיף יורד.
*(הערה: לולאת ה-play הידנית לא חסומה תחת reduced-motion — וזה נכון. המשתמש לחץ "נגן" במפורש.)*

### א2 — "CourtDiagram: ציור קווים" → **הרכיב לא מוצג בפועל**
כל שבעת הקוראים ל-`NotebookPage` מעבירים `noCourt`:
`DrillCard.jsx:239`, `DrillForm.jsx:381`, `PublicDrill.jsx:67`, `Schedule.jsx:346`, `TrainingPlans.jsx:745`, `TrainingPlans.jsx:1156`.
**מסקנה:** `CourtDiagram` הוא קוד מת כרגע. אנימציה עליו לא תיראה לאף אחד. הסעיף יורד.

---

## מה כן בוצע — איחוד שכבת הכניסה

**הבעיה שנמצאה:** בכל החלפת טאב רצו **שלוש** אנימציות כניסה זו על גבי זו:

| שכבה | סלקטור | אנימציה | מקור |
|---|---|---|---|
| 1 | `.main-inner` | `view-enter` — 380ms, opacity + `translateY(10px)` | `index.css:6224` |
| 2 | `.main-inner > *` | `fade-rise` — 320ms, opacity + `translateY(8px)` | `index.css:2753` |
| 3 | `.home-card` ואחרים | `card-enter` — 380ms + delay עד 190ms | `index.css:6239` |

המסך זז, מה שבתוכו זז שוב, והכרטיסים זזים בפעם השלישית — כ-570ms של תנועה מורכבת בכל ניווט,
מעל התקרה של **DESIGN.md §3** (כניסת מסך ≤380ms). זה מורגש בכל שמונת מסכי המאמן.

**התיקון (היררכיית תנועה — דבר אחד זז):** בלוק append מתוארך בסוף `src/index.css`:

1. `.main-inner` → `view-fade`, **fade בלבד בלי תזוזה**, `var(--dur-base)` (220ms).
2. `.main-inner > *` → `animation: none` — השכבה האמצעית מבוטלת. (ל-`.main-inner` יש בפועל שני ילדים בלבד, `QuoteStrip` והמסך, אז זה מעולם לא היה stagger אמיתי.)
3. הכרטיסים נשארים הרוקדים היחידים; ה-delay-ים הוזזו ל-200/250/300/350ms כדי שיתחילו **אחרי** שה-fade הסתיים במקום להתנגש בו.
4. כיבוי כפול מפורש — `@media (prefers-reduced-motion: reduce)` **וגם** `html.a11y-motion`, בלי להישען על כלל ה-`*` הגלובלי.

**סטטוס: אומת.** `npm run verify` רץ ועבר (28.7.2026).

### אימות שהכללים באמת מוחלים (ולא רק נכתבו)

| כלל | סלקטור | נמצא ב-JSX |
|---|---|---|
| `view-fade` | `.main-inner` | `Dashboard.jsx:319`, `PlayerDashboard.jsx:1799` — **עם `key={view}`**, כלומר הרכיב מתמאונט מחדש בכל טאב והאנימציה באמת רצה |
| `animation: none` | `.main-inner > *` | מנטרל את `fade-rise` על שני הילדים בפועל |
| `card-enter` + delays | `.home-card` `.drill-card` `.coach-card` `.land-feature` | Home, DrillCard/GamesBoard/TrainingPlans/CoachProfile, CoachFinder, Landing |

`fade-rise` לא התייתם — עדיין משמש `.drawer-overlay`, dropdown ועוד 2 מקומות.

**פער שנמצא ותוקן:** `.news-card` ו-`.stat-card` נכללו ברשימת `card-enter` (שורות 6237-6238) אבל **מעולם לא קיבלו `animation-delay`** — לא בגרסה הישנה ולא בשלב א'. הם נכנסו ב-delay 0, כלומר בדיוק בתוך ה-fade של `.main-inner` — ההתנגשות ששלב א' בא לפרק. תוקן בסולם עדין ותחום: **210 / 240 / 270 / 300ms** (מתחיל מיד אחרי ה-fade של 220ms, צעדי 30ms, נעצר ב-300). הם גם נשמטו מרשימת ההשבתה — נוספו לשני הכיבויים.

---

## שלב ב' — GSAP (בוצע 28.7.2026)

### התשתית: `src/anim.js`

מודול חדש שמרכז את חוזה התנועה ב-JS:

- `motionOff()` — `prefers-reduced-motion` **או** `html.a11y-motion`. הקוראים לא מאתחלים timeline בכלל כשזה true (כלל ה-`*` הגלובלי עם `transition-duration: 0.001ms` לא עוצר GSAP).
- `dur('--dur-slow')` — קורא את טוקני המשך מ-`index.css` וממיר לשניות. אין ms קשיח בקוד.
- `EASE_OUT = 'power4.out'` — המקבילה בזמן ריצה ל-`--ease-out` (`cubic-bezier(.22,1,.36,1)`).
- `loadGsap()` — `import()` דינמי ממוזער ל-promise יחיד, כולל `DrawSVGPlugin` (חינמי מ-GSAP 3.13). **gsap לא נכנס ל-bundle הראשי** ולא נטען בכלל למי שביקש פחות תנועה.
- `resetArrowDraw` / `clearArrowDraw` / `buildArrowDraw` — מנוע ציור החצים המשותף לשני המסכים.

**שני מנגנוני ציור, לפי גיאומטריה ולא לפי סגנון:**
- **חץ ישר** (תנועה + מסירה) — הארכת `x2/y2`. לא DrawSVG: חץ מסירה הוא `strokeDasharray`, ו-DrawSVG דורס בדיוק את התכונה הזו — התוצאה הייתה החלקת מקפים במקום קו נמתח. בונוס: ראש החץ נגרר עם הקצה בחינם.
- **חץ קשת** (זריקה לסל) — DrawSVGPlugin, וראש החץ מוסתר עד 90% מהציור כדי שלא ירחף על קו חלקי.
- `opacity: 0` עד שמגיע התור של החץ — בלי זה קו באורך אפס עדיין צייר את ראש החץ שלו, וראש חץ בודד ריחף על המגרש לאורך ההשהיה.

### ב1 — TacticsBoard: קווי תנועה שמציירים את עצמם

**ממצא:** שורה 610 עשתה `arrows: []` — במצב "נגן אנימציה" לא היו חצים **בכלל**. זו תוספת, לא החלפה.

**שילוב ולא דריסה:** לולאת ה-`requestAnimationFrame` (531-555) נשארה **השעון היחיד**. GSAP מקבל timeline `paused: true` שנגרר ידנית ב-`tl.progress(frame.p / 0.85)` מתוך `useArrowDraw`. אין שתי לולאות שנגררות זו מזו, ו"השהה"/"התחל מהתחלה" הקיימים עובדים בלי שורת קוד נוספת כי הם משנים את `frame`.

- `DRAW_LEAD = 0.85` — החצים מסיימים להצטייר ב-85% מהמעבר: הקו מוביל, השחקן עוקב.
- `resetArrowDraw` מוחל ב-`useLayoutEffect` (סינכרוני) כדי שלא יהיה הבזק של חצים מלאים בזמן שה-`import()` בדרך.
- תלויות ה-effect הן חתימת ה-id-ים ולא מערך החצים — אחרת ה-timeline היה נבנה מחדש 60 פעם בשנייה.
- `clearArrowDraw` עובר על `geom` ולא על ה-DOM: בהחלפת שלב React כבר החליף את החצים לפני שה-cleanup של השלב הקודם רץ, ומעבר על ה-DOM היה מנקה את מצב הפתיחה של השלב **החדש**.

### ב2 — CourtDiagram: ציור הדרגתי (אפשרות ב')

`useNotebookDraw` — במאונט: קווי המגרש נמתחים (DrawSVG, stagger) → החצים מציירים את עצמם → האובייקטים נכנסים ב-fade+scale. כניסה מדורגת בין תרשימים סמוכים, חסומה ב-3 כדי שלא יהיה זנב אינסופי בתוכנית ארוכה. מאזין `beforeprint` קופץ ל-`progress(1)` כדי שהדפסה באמצע האנימציה תיתן דף שלם.

**הרכיב הופעל בפועל** (זו הייתה אפשרות ב' — שינוי מוצר, לא רק אנימציה): `noCourt` הוסר מ-`DrillCard.jsx` ומ-`PublicDrill.jsx`. הנימוק מעבר לאסתטיקה: כלל ה-`@media print` מסתיר הכול חוץ מ-`.notebook`, ו-`TacticsBoard` יושב **מחוץ** לה — כלומר עד היום **הדפסת תרגיל איבדה את המגרש לגמרי**. עמודת ה-150px היא storyboard מודפס לצד הנגן האינטראקטיבי.

לא הופעל ב: `DrillForm` (תצוגה מקדימה חיה תוך כדי עריכה על הלוח), ובשלושת מסכי התוכנית (`plan.board` לא קיים ב-`planToNotebook` — היה מתקבל מגרש ריק חסר ערך; לכל פריט כבר יש `TacticsBoard` משלו).

**`DrillSketch.jsx` — דולג.** 0 ייבואים בכל הריפו. קוד מת.

### אימות (28.7.2026)

`npm run verify` עבר. בנוסף — בדיקה חיה בדפדפן על עמוד probe זמני עם נתוני בדיקה (נמחק אחרי):

- **CourtDiagram**: שלושת התרשימים מציירים את עצמם ומסיימים נכון — `strokeDashoffset: 0`, חצים ב-`x2` היעד (250 / 380 / 150), ראש החץ הכתום מוחזר, אובייקטים ב-opacity 1.
- **TacticsBoard**: נתפס באמצע גרירה — חץ התנועה ב-`x2=231.06, y2=226.22` (בין המוצא 120,380 ליעד 250,200), חץ המסירה שתורו טרם הגיע ב-`opacity: 0` וקצה מקופל. כלומר השרשרת `frame.p → drawScrub → tl.progress → DOM` עובדת.
- **חוזה הנגישות**: תחת `html.a11y-motion` — `performance.getEntriesByType('resource')` מחזיר **אפס בקשות ל-gsap**. לא רק שה-timeline לא רץ; הספרייה לא הגיעה לדפדפן. כל התרשימים והחצים מוצגים שלמים וסטטיים, בלי opacity או dasharray אינליין.
- הערה: הכרטיסייה הייתה מוסתרת ולכן `requestAnimationFrame` היה חנוק לחלוטין (0 פריימים/שנייה). האנימציות "נתקעו" — זה חניקת דפדפן, לא באג. האימות בוצע בגרירה ידנית של `globalTimeline.time()`.

**גדלים:** ה-bundle הראשי גדל ב-3.9KB בלבד (315.1 → 319.0KB). gsap יושב ב-chunk עצל נפרד של 70.8KB + 4KB ל-DrawSVGPlugin, שנטענים רק כשבאמת מנפישים.

---

## שלב ג' — motion (הוצע 28.7.2026, **ממתין לאישור**)

המטרה: שכל האפליקציה תרגיש נעימה, לא רק לוח הטקטיקה.

### ממצאי סקר לפני הכתיבה

*(אומת מחדש 28.7.2026: `btn-primary` ×135 מופעים, `icon-btn` ×44, 15 קבצים עם `disabled={busy|saving|loading}`, 8 מופעי `tm-overlay`, 0 ייבואי `motion`. `Button.jsx` / `Modal.jsx` / `Sheet.jsx` — עדיין לא קיימים.)*

| ממצא | מספרים |
|---|---|
| כפתורים הם **שכבת CSS, לא קומפוננטה** | `btn-primary` ×50, `btn-ghost` ×39+10, `icon-btn` ×24, `btn-soft` ×18 — סה"כ ~180 אתרי קריאה |
| חלק ניכר מהחוזה **כבר קיים** | `index.css:6262` — hover lift, `scale(0.985)` בלחיצה, טבעת `:focus-visible` |
| `.btn-spinner` קיים (`index.css:8726`) | אבל **מחווט ידנית** ב-9 מקומות, ו-28 קבצים עושים `disabled={busy}` בלי `aria-busy` עקבי |
| `motion` בכלל לא מיובא | 0 ייבואים בריפו, למרות `motion@12.42.2` ב-`package.json` |
| `save-pop` קיים (`index.css:7250`) | תקדים למיקרו-פידבק — להרחיב, לא להמציא מחדש |

### ג1 — כפתורים: מערכת אחת

הפערים האמיתיים אינם "אין מערכת" אלא שלושה חורים בה:
1. **`.icon-btn` מחוץ למערכת** — `transition: all 0.15s ease-out` קשיח (`index.css:2588`), בלי טוקן, בלי scale בלחיצה, בלי הרמה.
2. **`btn-primary` מוגדר ארבע פעמים** — שורות 303 / 3357 / 6263 / 6505. מלחמת ספציפיות.
3. **מצב טעינה ידני** — ספינר, `disabled`, `aria-busy` ורוחב הכפתור (הטקסט קופץ בין "שמור" ל"שומר...") מחווטים מחדש בכל אתר.

העבודה: (א) בלוק append יחיד — `icon-btn` וה-one-off-ים המנוקבים נכנסים למערכת, טוקנים במקום `0.15s`, `:disabled` אחיד. (ב) `src/Button.jsx` דק שמחזיק רק את מה ש-CSS לא יכול: `loading` → ספינר + `aria-busy` + נעילה אוטומטית + **רוחב נעול** כדי שהכפתור לא יקפוץ.

**החלטה 4 (28.7.2026): היקף `Button.jsx` — כפתורי async בלבד.**
הקומפוננטה מחליפה רק את ~37 הכפתורים עם `loading`/`disabled` — שם הכאב האמיתי. ~145 הכפתורים הרגילים נשארים על המחלקות ומקבלים את השיפור מבלוק ה-CSS. הנימוק: מיגרציה מלאה היא ~180 שינויים על ~40 קבצים, כל אחד עם מחלקות/aria/ref משלו, **בלי טסטים אוטומטיים לתפוס רגרסיה** — עלות-סיכון שלא מחזירה ערך, כי שכבת ה-CSS כבר נותנת לכפתור הרגיל את ההתנהגות הנכונה.

### ג2 — מודאלים ומגירות

`LazyMotion` + `domAnimation` + `m.*` בייבוא עצל, חסום ב-`motionOff()` מ-`anim.js` — למי שביקש פחות תנועה `motion` **לא מגיע לדפדפן בכלל**, בדיוק כמו gsap בשלב ב'.

- `Modal.jsx` על דפוס `tm-overlay`/`tm-modal` (8 מופעים: `Teams` ×5, `DrillLibrary` ×2, `ReportButton`) — `useFocusTrap` כבר קיים ומחובר.
- `Sheet.jsx` ל-`FeedbackSheet` — גרירה למטה לסגירה, **רק תחת `pointer: coarse`**.
- מגירה (`Dashboard.jsx:236`, `PlayerDashboard.jsx:1768`) — כניסה מהצד הלוגי. ב-RTL זה **ימין**, מחושב מ-`dir` ולא `x: -100` קשיח.

### ג3 — מעברי layout

`layout` על רשימות **קצרות ומשתנות בלבד**: `MultiSelect` (צ'יפים), `TeamAssignments`, `PlanRunner`.
**במפורש לא**: `DrillLibrary`, `Messages`, `TrainingPlans`, `Attendance` — רשימות ארוכות עם גלילה. הכלל: `layout` רק מתחת ל-~25 פריטים, ורק כשהרשימה באמת מסתננת/ממיינת/נמחקת.

### ג4 — מיקרו-פידבק

הרחבת התקדים הקיים (`save-pop`), לא מנגנון חדש:
- **שמירה** — הכפתור הופך ל-✓ לכ-1.2ש'; toast רק כשהתוצאה חוצה מסך.
- **הוספת שחקן** — השורה החדשה נכנסת ב-fade+rise עם הדגשה חולפת.
- **סימון נוכחות** — פעימת לחיצה על המתג.
בלי קונפטי בעולם המאמן (DESIGN.md §0 — "רציני-פרימיום").

### חוזה
כיבוי כפול · טוקנים בלבד · RTL לוגי · CSS append-only · `npm run verify`.

### סדר ביצוע מוצע (4 קומיטים, כל אחד עומד בפני עצמו)

| # | קומיט | קבצים | מה נכנס |
|---|---|---|---|
| ג1 | מערכת כפתורים | בלוק append ב-`index.css` + `src/Button.jsx` חדש | `.icon-btn` נכנס למערכת (טוקנים במקום `0.15s` קשיח, `scale(.985)` בלחיצה, הרמה ב-hover, `:focus-visible` אחיד, `:disabled` אחיד); איחוד ארבע ההגדרות הכפולות של `btn-primary`; `<Button loading>` = ספינר + `aria-busy` + נעילה + **רוחב נעול** (הטקסט לא קופץ). מיגרציה: ~15 הקבצים עם `disabled={busy}` בלבד. |
| ג2 | מודאלים ומגירות | `src/Modal.jsx`, `src/Sheet.jsx`, `Teams` ×5, `DrillLibrary` ×2, `ReportButton`, `FeedbackSheet`, `Dashboard.jsx:236`, `PlayerDashboard.jsx:1768` | `LazyMotion`+`domAnimation`+`m.*` בייבוא **עצל**, חסום ב-`motionOff()` — כמו gsap, למי שביקש פחות תנועה הספרייה לא מגיעה לדפדפן. `AnimatePresence` על 8 המודאלים (overlay fade + מודאל scale/rise). גרירה למטה לסגירה **רק** תחת `pointer: coarse`. מגירה — נכנסת מהכיוון הלוגי (RTL=ימין), מחושב מ-`dir`, לא `x:-100`. |
| ג3 | מעברי layout | `MultiSelect`, `TeamAssignments`, `PlanRunner` | `layout` על רשימות קצרות בלבד (<~25 פריטים, ורק כשהן מסתננות/ממיינות/נמחקות). **מפורשות בחוץ**: `DrillLibrary`, `Messages`, `TrainingPlans`, `Attendance` — רשימות ארוכות עם גלילה. |
| ג4 | מיקרו-פידבק | הרחבת `save-pop` (`index.css:7250`) + נקודות שמירה/הוספת שחקן/נוכחות | שמירה → הכפתור הופך ל-✓ לכ-1.2ש' (toast רק כשהתוצאה חוצה מסך); שחקן חדש → fade+rise עם הדגשה חולפת; נוכחות → פעימת לחיצה על המתג. בלי קונפטי בעולם המאמן. |

**סיכון עיקרי ואיך מנוטרל:** DESIGN.md §3 מתעד ש-`AnimatePresence mode="wait"` על החלפת views נתקע בטאבים ברקע. לכן `motion` נוגע **רק** במודאלים/גיליונות/רשימות — אפס נגיעה בכניסות מסך, שנשארות CSS משלב א'.

---

## הבא בתור (לא אושר עדיין)

- **שלב ד' — פוליש**: פסי התקדמות `width` → `scaleX` (כ-20 מופעים ב-`index.css` מפרים את DESIGN.md §3: "אין אנימציה של width/height בלייאאוט"); Lenis בדפים הציבוריים.
- ~~ספירת מספרים~~ — נדחה סופית (החלטה 3).

## חוזה קבוע לכל שלב
- כיבוי כפול: `prefers-reduced-motion` **וגם** `html.a11y-motion`. ב-JS זה אומר **לא לאתחל את ה-tween בכלל** — `transition-duration: 0.001ms !important` לא עוצר GSAP או motion.
- CSS חדש = בלוק append עם banner מתוארך בסוף `index.css` בלבד.
- טוקנים בלבד: `--dur-fast/base/slow`, `--ease-out/spring`. אין ms קשיח.
- RTL: תזוזה אופקית בכיוון לוגי, לא `x: -100`.
- `npm run verify` אחרי כל שלב.

## חוב שנצפה אגב הדרך (לא טופל)
- `Schedule.jsx:341` מייבא `ArrowRight` ישירות — DESIGN.md §4 מחייב `src/DirIcon.jsx`.
- `CourtDiagram.jsx` מכיל hex גולמי (`#1b2a4a`, `#E8763A`, `#D64545`) — DESIGN.md §1. גם `TacticsBoard.jsx` (`#1B2A4A`, `#E3B877`).
- `DrillSketch.jsx` — קוד מת, 0 ייבואים. מועמד למחיקה.
- כל `CourtDiagram` מגדיר מחדש `<marker id="nb-arrow">` — id כפול ב-DOM כשיש כמה תרשימים בעמוד. ויזואלית לא מורגש (המרקרים זהים), אבל לא תקין. קדם-קיים.

---

# שלב ד' — תמונות + אפקטים (הוצע 28.7.2026, **ממתין לאישור**)

## חלק 1 — מערכת תמונות מ-Openverse

### מה נבדק בפועל לפני כתיבת התוכנית (curl חי מול ה-API)

| בדיקה | תוצאה |
|---|---|
| `GET /v1/images/?q=...&license=cc0,pdm` בלי מפתח | **200 עובד**. אין צורך במפתח. |
| שדות `width`/`height` בתשובת החיפוש | קיימים — הסינון ל-1200px אפשרי בלי בקשה נוספת |
| `basketball court` — עמוד אחד של 20 | **0** תמונות ≥1200px |
| `basketball training` — 120 תוצאות (6 עמודים) | **1** תמונה ≥1200px |
| `basketball team` — 120 תוצאות | 49 ≥1200px |
| `basketball hoop` — עמוד אחד | 17 ≥1200px |

**המסקנה:** רוב תמונות ה-CC0 ב-Openverse מגיעות מ-Flickr ומוגבלות ל-1024px. שאילתה אחת + עמוד אחד לא מספקת 8 תמונות. שתי התאמות נדרשות לספק:

1. **דפדוף** — הסקריפט מושך עמוד אחרי עמוד (`page_size=20` כפי שנקבע) עד שנאספו 8 עוברות-סינון, עד תקרה של 8 עמודים לשאילתה.
2. **שרשרת שאילתות גיבוי לכל קטגוריה** — `drills` על `basketball training` בלבד מחזירה תמונה אחת. `basketball practice` מחזירה **42 מתוך 60**. אז: שאילתה ראשית + גיבויים, ממשיכים לשאילתה הבאה רק אם עוד חסרות תמונות.

| קטגוריה | שאילתה ראשית | גיבויים |
|---|---|---|
| `hero` | `basketball court` | `basketball arena`, `outdoor basketball court` |
| `drills` | `basketball training` | `basketball practice`, `basketball players` |
| `community` | `basketball team` | `basketball players` |
| `articles` | `basketball hoop` | `basketball backboard` |

### תלות חדשה: `sharp`

אין דרך להמיר ל-WebP בלי ספריית תמונות. `sharp` נכנס כ-**devDependency בלבד** (`npm i -D sharp`) — רץ רק ב-`npm run images` על המכונה, לא נכנס ל-bundle ולא לפרודקשן. "בלי מפתח API" מתקיים; זו לא הפרה של הדרישה אלא הכלי שמבצע אותה.

### `scripts/fetch-images.mjs`

- `fetch` מובנה של Node (18+), `User-Agent` מזוהה, השהיה קצרה בין בקשות (כיבוד rate-limit אנונימי).
- סינון: `width >= 1200` · דדופ לפי `id` · דילוג על `mature` · דילוג על תמונות שלא נטענות.
- לכל תמונה שני קבצים ב-`public/images/<קטגוריה>/`: `<slug>-1600.webp` ו-`<slug>-800.webp` (`quality: 78`, `withoutEnlargement`).
- **בנוסף — LQIP:** גרסת 24px מוקטנת מוטמעת כ-data-URI ישירות ב-`images.json`. זה מה שנותן "placeholder מטושטש בלי קפיצת layout" בלי בקשת רשת שלישית.
- `src/data/images.json` לכל פריט: `src` (1600), `srcSmall` (800), `w`, `h`, `lqip`, `alt`, `license`, `licenseUrl`, `creator`, `sourceUrl`.
- אידמפוטנטי: קובץ שכבר קיים לא יורד שוב. `npm run images` ב-`package.json`.

**`alt` בעברית:** כותרות Openverse הן אנגלית. אין תרגום מכונה בסקריפט (יוליד עברית שבורה). במקומו: תבנית עברית קבועה לכל קטגוריה — `hero`: "מגרש כדורסל", `drills`: "שחקני כדורסל באימון", `community`: "קבוצת כדורסל", `articles`: "סל כדורסל" — עם מספר סידורי לייחוד. אחרי ההרצה **אעבור על 32 התמונות בעיניים** ואחליף כל תמונה לא-רלוונטית או בעייתית (Openverse מחזיר תוצאות בלתי צפויות), ואדייק את ה-alt לתמונות שדורשות זאת.

### `src/SmartImage.jsx`

```
<SmartImage category="hero" priority />
```
- מגריל פריט מהקטגוריה **פעם אחת ב-mount** (`useState(() => pick())`) — לא בכל רנדר, אחרת התמונה תקפוץ בכל setState.
- `<img>` עם `srcSet` (800w/1600w) + `sizes`, `loading="lazy"` (`eager` כש-`priority`), `decoding="async"`, `width`/`height` אמיתיים → **אפס CLS**.
- ה-LQIP כ-`background-image` מתחת, מתעמעם החוצה ב-`onLoad`. תחת reduced-motion — החלפה מיידית בלי fade.
- `scrim`: שכבת `linear-gradient` נייבי מטוקנים מעל התמונה. יעד ניגודיות 4.5:1 — אמדוד בפועל על הטקסט הלבן ואכוונן את האטימות.

**שימוש:** `hero` → דף הבית · `drills` → כרטיס פוסטר בראש `DrillLibrary` · `community` → ראש `Community` · `articles` → fallback ל-`.news-thumb` בלי תמונה (היום מוצג שם אייקון עיתון בלבד).

### הירו של דף הבית — הנקודה שדורשת החלטה

`home-hero` היום הוא **דפוס #1 של DESIGN.md §2ב**: גרדיאנט נייבי + סימן-מים SVG של מגרש. תמונה מלאה תמחק את הזהות הזאת.

**ההצעה:** התמונה נכנסת **מתחת** לגרדיאנט הנייבי הקיים (הגרדיאנט נשאר ומקבל אטימות ~0.78), לא במקומו. ה-SVG של המגרש נשאר. התוצאה: אותו הירו נייבי, עם עומק אמיתי מאחוריו. שלוש תמונות מתחלפות כל 7 שניות ב-crossfade של 400ms (שתי שכבות `<img>` מוצלבות — לא `background-image`, כדי לשמור על lazy-loading ו-`srcSet`).

**הציטוט:** `QuoteStrip` הקיים כבר מתחלף כל 7 שניות מ-`COACHING_QUOTES`, אבל הוא יושב ב-shell מעל `.main-inner` ולא בהירו. אכניס אליו את הציטוט הנוכחי כרצועה בתחתית ההירו, **מסונכרן לאותו טיימר של התמונה** (תמונה + ציטוט מתחלפים יחד).

---

## חלק 2 — אפקטים בכל האפליקציה

### מה כבר קיים ולא ייבנה מחדש

בדיקה מול הקוד לפני התכנון:

| הבקשה | המצב בפועל |
|---|---|
| 6. אנימציית כניסה/יציאה ל-toasts | **קיים ומלא.** `toast-in` (`index.css:2511`) + `toast-out` (`6334`) + `EXIT_MS=200` מסונכרן ב-`Toaster.jsx:8`. |
| 5. הרמת כרטיסים ב-hover | **קיים** על `.home-card` `.drill-card` `.coach-card` `.news-card` `.video-card` `.podcast-card`. |
| 3. skeletons | **קיים** — `Skeleton.jsx` עם 5 שלדים תואמי-צורה. |
| 1. מעברי ניווט | **קיים** מ-שלב א' — `view-fade` על `.main-inner` עם `key={view}`. |
| 8. `burstConfetti` | **קיים ומחובר** ב-`PlayerDashboard:500,516` ו-`PlayerGoals:256`. |
| 2. scroll-reveal | קיים **רק ב-Landing** (`Landing.jsx:24-30` + `index.css:6246`) — יש דפוס להכללה. |

אז חלק 2 הוא בעיקר **הכללה וסגירת פערים**, לא בנייה מאפס. הפערים האמיתיים:

| # | הפער | העבודה |
|---|---|---|
| 2 | scroll-reveal רק בנחיתה | `src/useReveal.js` — IntersectionObserver משותף אחד (`rootMargin: -10%`, `once`). מוחל על **מקטעי-על בלבד** בארבעת הדפים הארוכים (Home, DrillLibrary, Community, PlayerDashboard) — לא על פריטי רשימות. `motionOff()` → לא מאתחל observer בכלל. |
| 4 | סימון פעיל בסיידבר קופץ | ה-`::before` הכתום (`index.css:6380`) קופץ בין פריטים. פס יחיד שמוזז ב-`transform: translateY` עם `--dur-base` — הסימון גולש. אין `layout` של motion; CSS טהור. |
| 5 | הרמת כרטיסים לא אחידה | סולם צל מדורג אחיד (`--shadow-sm` → `--shadow-md`) + `transition` על טוקנים במקום ה-`0.15s`/`all` הקשיחים שפזורים היום. **בלי** `transition: all`. |
| 7 | אין פרלקסה בהירו | `translate3d` על שכבת התמונה בלבד, **תקרה 40px**, rAF מצומצם, מנוטרל תחת `pointer: coarse` (מובייל) ותחת `motionOff()`. |
| 8 | אין חגיגה בסיום אימון במאמן | ראה החלטה פתוחה למטה. |
| 3 | 15 מסכים עם `loading` בלי שלד | מיפוי `loading &&` → השלד תואם-הצורה הקיים, במסכים שעדיין מציגים ריק/ספינר. |

### חוזה
כיבוי כפול (`prefers-reduced-motion` **וגם** `html.a11y-motion`) · כל אפקט ≤400ms · טוקנים בלבד · אפס אנימציה על פריטי רשימות ארוכות · CSS append-only · `npm run verify`.

### סדר ביצוע (5 קומיטים)

| # | קומיט | מה נכנס |
|---|---|---|
| ד1 | תשתית תמונות | `sharp` כ-devDep · `scripts/fetch-images.mjs` · `npm run images` · **הרצה בפועל** · `public/images/**` · `src/data/images.json` · בדיקה ידנית של 32 התמונות |
| ד2 | `SmartImage` | `src/SmartImage.jsx` + בלוק CSS (scrim, LQIP, aspect) |
| ד3 | שילוב בארבעת הדפים | הירו מתחלף + ציטוט מסונכרן · פוסטר בתרגילים · ראש קהילה · fallback לכתבות |
| ד4 | תנועה — סיידבר, כרטיסים, reveal | `useReveal.js` + בלוק CSS: פס סיידבר גולש, סולם צל אחיד, scroll-reveal במקטעי-על |
| ד5 | פרלקסה + שלדים + חגיגה | פרלקסה בהירו · השלמת שלדים · חגיגת סיום אימון |

## החלטות שנדרשות לפני התחלה

1. **הירו של דף הבית** — תמונה מתחת לגרדיאנט הנייבי (שמירת דפוס #1), או תמונה במקום הגרדיאנט?
2. **קונפטי בעולם המאמן** — DESIGN.md §0 ו-PLAN.md ג4 אוסרים במפורש קונפטי בעולם המאמן ("רציני-פרימיום"). בעולם השחקן הוא כבר עובד. האם החגיגה בסיום אימון היא (א) בעולם השחקן בלבד — קונפטי, ובמאמן ✓ מודגש ורגוע; (ב) קונפטי בשניהם, כלומר שינוי מודע של DESIGN.md §0?
3. **פרלקסה** — DESIGN.md §3 אוסר "parallax כבד". ההצעה (40px, transform בלבד, כבוי במובייל) היא בגבול הכלל. לאשר כחריג מתועד?
