# CourtSide — האנדוף 6.8.2026

מסמך מעבר לצ'אט חדש. הסשן הזה כלל **חמישה גלים**: עטיפה לאנדרואיד, לוח
הלו״ז לדסקטופ, אימון אישי מקצה לקצה, עיצוב מחדש של בית השחקן, ושורה של
תיקוני אבטחה שצצו בדרך.

**הכול נדחף. `main` = `redesign`.** חמש מיגרציות חדשות ממתינות/רצו — ראו §4.

---

## 0) לקרוא קודם

| קובץ | למה |
|---|---|
| `CLAUDE.md` | קונבנציות מחייבות. index.css append-only, `npm run verify` אחרי כל שינוי |
| `HANDOFF_4.8.md` | הסשן הקודם — אבטחה, הסכמת הורים, Capacitor |
| `הרצת_SQL_3.8.md` | צ'קליסט ההרצה |
| `DESIGN.md` | שפה עיצובית. **§3 כאן מוסיף לה שכבה** |

**איך לדבר עם הבעלים:** עברית פשוטה, בלי ז'רגון, **צעד אחד בכל פעם**.
הוא מפעיל את המוצר ומריץ SQL בעצמו אך אינו מהנדס.

⚠ **והוא צודק כשהוא אומר «זה לא עובד».** שלוש פעמים בסשן הזה טענתי
«באוויר» כשזה לא היה מדויק, והוא תפס בכל פעם. ראו §5.

---

## 1) מה נבנה

### א. אפליקציית אנדרואיד
Capacitor 8.5, `il.courtside.app`, "Courtside.il". `npm run app:build`
מחזיר APK. אייקון ומסך פתיחה מהלוגו.

⚠ **JDK 21 חובה.** Android Studio מגיע עם 25 ו-Gradle 8.14.3 לא קורא
אותה. הנתיב ב-`~/.gradle/gradle.properties`, מחוץ לריפו.

⚠ **האפליקציה לא מתעדכנת מדחיפה לאתר.** היא נושאת עותק קפוא. כל בדיקת
פיצ'ר חדש חייבת להיות **בדפדפן**, לא באפליקציה.

### ב. לוח הלו״ז של המאמן (מסך רחב)
מעל 1100px: שני טורים, לוח כמשטח ראשי, טור צדדי דביק (הוספה מהירה ·
הבא בתור · ימי אימון קבועים · מדידות). גרירה על עמודה ריקה פותחת אירוע
בטווח מדויק, קפיצות 15 דקות, רק בעכבר. משחקים מצוירים על הלוח לראשונה.

**מתחת ל-1100px שום דבר לא זז.**

### ג. אימון אישי — מקצה לקצה
מאמן ↔ מתאמן, נפרד מהקבוצה. `personal_trainees`. השחקן מבקש בקוד אישי
(נפרד מקוד הקבוצה), המאמן מאשר, **וקטין נעול עד שההורה מאשר את המאמן
הזה בשם**. המאמן מנפיק את הקישור להורה ומעביר בוואטסאפ.

מנוע המשימות לא נבנה — `player_assignments` כבר היה קיים.

### ד. בית השחקן — עיצוב חדש
מבנה «עיתון»: טור ראשי + סרגל צד דביק. באנר עם רקע לילה, שתי קשתות,
פס «מגיע לאימון?» חם ולוח תוצאות. סולם מאמץ 1–10 מהיר בבאנר.
במובייל: לוח 2×2, לו״ז כרצועה אופקית עם scroll-snap.

---

## 2) ⚠ ארבעה פערי אבטחה שנסגרו

1. **זימון פגישה יכול היה להישלח לקטין.** המדיניות בדקה רק שהשולח הוא
   עצמו. מדיניות הקריאה של profiles מחזירה למאמן גם את שחקניו — ולכן
   הבורר הציג ילדים. נסגר ב-`supabase_schedule_board_4_8.sql`.
2. **`player_assignments` בדקה רק `coach_id = auth.uid()`** — מאמן יכול
   היה לשגר משימה לכל מזהה שהוא מכיר. עכשיו: לקבוצה שלו או למתאמן
   אישי פעיל בלבד.
3. **`PlayerTimeline` ביקש עמודה שלא קיימת** — PostgREST הפיל את כל
   השאילתה, והלו״ז נעלם מהציר של השחקן.
4. **`gitignore` של Capacitor משאיר קבצי חתימה במעקב.** הריפו ציבורי,
   ומפתח חתימה שדולף אינו ניתן לביטול.

---

## 3) שפת הצבע — הכלל שנוסף

**חם = תורך לפעול · קר = כבר נקבע · ירוק = סגור.**

| חם (כתום/רוסט) | קר (נייבי) | ירוק |
|---|---|---|
| משימות · משוב · אישור הגעה · מאמן אישי ממתין | לו״ז · יעדים · סיכום המאמן | בוצע · אושר · נרשם |

הצבע יושב על **ריבוע קטן לפני תווית המקטע**, לא על הטקסט.

⚠ **הטיפוגרפיה לא זזה.** `.pl-section-label` נשארת 12.5px / 800 /
0.04em. זו הייתה הערת הבעלים על הפרוטוטייפ והיא נכונה — תווית שמשנה
גודל וצבע בין מסכים מפרקת את אחידות המוצר.

**הפרוטוטייפים** בפרויקט claude.ai/design `856db763-9314-448d-b74d-55e2234fdde9`:
`Courtside Player Home Web.dc.html` · `…Web v2.html` · `…v3 (web + mobile).html`
· `…v4 (effort + polish).html` ← **המאושר**.

---

## 4) מה נשאר לבעלים

### להריץ (לפי הסדר)
| קובץ | סטטוס |
|---|---|
| `supabase_schedule_board_4_8.sql` | רץ |
| `supabase_personal_training_4_8.sql` | רץ |
| `supabase_personal_code_4_8.sql` | רץ |
| `supabase_trainee_consent_4_8.sql` | ⚠ **תוקן פעמיים — לוודא שרץ בגרסה האחרונה** |

### לאמת
**המובייל של בית השחקן** — לוח 2×2 ורצועת הלו״ז האופקית. **לא אומת חי.**

**סולם המאמץ** — מופיע רק אחרי אימון שהסתיים היום ולא דורג.

### לא נבנה
מסלול ההורה עובד, אבל **אין תשתית מייל** — הקישור עובר ידנית.
Push notifications · iOS (דורש Mac) · פיצול קבצי ענק.

### חוסם השקה
**עורך דין.** ועכשיו גם על אימון אישי בתשלום לקטינים.

---

## 5) ⚠ מלכודות — כל אחת עלתה זמן בסשן הזה

**«נדחף» ≠ «נפרס» ≠ «אומת בעין».** להפריד תמיד ולומר איזה מהם.

**`curl` רגיל משקר.** הוא מקבל עותק שמור של Vercel (ראיתי `Age: 2601`).
חייבים `-H 'Cache-Control: no-cache'` ו-`?cb=$$`. **וקוד של מסך אינו
ב-`index.js`** אלא בקטע נפרד — חילוץ שם הקטע מה-index ואז חיפוש בו.

**שקט אינו כישלון.** קריאה לתוסף נייטיב יכולה לא לחזור כלל. `try/catch`
תופס רק דחייה. כל קריאה שחוצה גשר חייבת `Promise.race` מול טיימר.

**`rows === null` הוא באג.** רכיב שמחזיר `null` עד שהשרת עונה נעלם
לגמרי בכל תקלה. **להתחיל ממערך ריק.**

**pgcrypto אינו בנתיב החיפוש** של פונקציה עם `set search_path = public`.
להשתמש ב-`sha256(convert_to(...))` המובנית וב-`public.new_consent_token()`.

**`profiles` — הרשאה ברמת עמודה.** עמודה אחת מחוץ ל-whitelist מפילה את
**כל** השאילתה. הרשימה: `id, first_name, last_name, club, age_groups,
avatar_url, verified, banned, role, position, phone_public, is_admin,
created_at, updated_at`.

**הודעת שגיאה שמנחשת גרועה משגיאה גולמית.** `/function .* does not
exist/` תפסה שגיאה **מתוך** פונקציה והציגה «הפיצ'ר לא הופעל» על מסד
תקין. רק `PGRST202` פירושו «ה-RPC חסר».

**רענון ידני השבית את ההתאוששות מדפלוי** — `chunkFlag.read()` ספר
`Ctrl+Shift+R` של המשתמש כ«כבר ניסינו».

**index.css append-only.** הקובץ ~24,000 שורות. תיקון = בלוק מתוארך בסוף.
