# הרצת SQL — 12.9.2026 (הצ'ק-אין: מה שההורה חתם עליו, ומה שנעלם לך מהמסך)

שלושה קבצים, בסדר הזה. כל שלב עומד בפני עצמו — אפשר לעצור אחרי כל אחד
מהם, ומה שרץ עד שם נשאר תקין. אחרי כל שלב יש **שאילתת בדיקה אחת**:
מדביקים אותה ב-SQL Editor, מריצים, ומסתכלים על התשובה.

**איפה מריצים:** Supabase → הפרויקט שלך → `SQL Editor` → `New query` →
מדביקים את **כל** תוכן הקובץ → `Run`.

---

## למה זה דחוף — בשפה שלך

**הילדים עונים כל בוקר על שאלות שההורים לא אישרו.**
הכרטיס בבוקר שואל כמה שעות ישן, איך האנרגיה, איפה כואב, והאם הוא חולה.
זה מידע בריאותי על ילד בן 11–17. המסמך שההורה חתם עליו בפועל הוא נוסח
ישן שכתוב בו מה נאסף — ו**המילה «צ'ק-אין» לא מופיעה בו אפילו פעם אחת**.
הנוסח החדש, זה שמסביר את שאלות הבוקר בסעיף שלם, כתוב ומוכן בקובץ —
אבל הוא מעולם לא הורץ, ולכן ההורים לא ראו אותו. כל הסכמה שנחתמת היום
מצביעה על הנוסח הישן. **שלב 1 סוגר את זה, והוא הדבר הראשון שצריך לקרות.**

אם אתה לא יכול להריץ עכשיו — ההסכמה הנכונה היא **לכבות בינתיים את
כרטיס הבוקר** ולא להמשיך לאסוף. אין מצב ביניים תקין.

---

## שלב 1 — הנוסח שההורה קורא (`supabase_legal_v3_4_9.sql`)

**מה זה עושה:** מחליף שלוש שורות טקסט בטבלת המסמכים: תנאי השימוש,
מדיניות הפרטיות, ומסמך ההורה. מסמך ההורה מקבל סעיף 4 שלם שמסביר את
שאלות הבוקר — מה נשאל, למה, מי רואה, כמה זמן זה נשמר, ואיך מכבים.

**מה זה לא עושה:** לא נוגע באף הסכמה שכבר נחתמה (אלה ראיות, הן נשארות
כמו שהן), לא משעה אף חשבון, ולא חוסם אף ילד.

**שלוש תופעות לוואי שתראה — כולן מכוונות:**
1. לילדים שכבר אושרו יופיע באנר רך «לאשר מחדש». אף אחד לא נחסם.
2. אם תשלח להורה קישור הסכמה **חדש**, הטופס ייפתח בלי סימונים — כלומר
   הסכמות רשות שההורה כבר נתן עלולות לרדת. לחידוש השתמש ב**קישור
   הניהול** שנמסר לו, לא בקישור ראשוני חדש.
3. «עולם הכדורסל» עדיין מוסתר, ולכן שום דבר שם לא ישתנה בפועל.

**⚠ ואזהרה אחת שאינה במסד:** באפליקציית האנדרואיד שהתקנת על הטאבלט
עדיין מוטבע הנוסח הישן. אחרי ההרצה צריך לבנות APK חדש
(`npm run build` ואז `npx cap sync android`) ולהתקין אותו. עד אז, מי
שנרשם **מתוך האפליקציה** יחתום על מספר גרסה ישן. אם אי אפשר לבנות
עכשיו — אפשר להריץ בכל זאת, רק תדע שזה המצב.

**שאילתת בדיקה (אחרי שלב 1):**

```sql
select id, version, updated_at
  from public.consent_documents
 order by id;
```

צריך לראות שלוש שורות:
`doc_a` → `v2 · 6.9.2026`, `privacy` → `v3 · 6.9.2026`, `terms` → `v3 · 6.9.2026`.
אם `doc_a` עדיין `v1 · 3.8.2026` — הקובץ לא רץ.

**ביטול:** בתוך הקובץ עצמו, בסוף, יש בלוק «ביטול» עם ההוראה המדויקת
(צריך להחזיר את מספר הגרסה **ואת הנוסח יחד** — אסור להחזיר רק אחד מהם).

---

## שלב 2 — תיקוני הפיילוט (`supabase_pilot_fixes_6_9.sql`)

הקובץ הזה נכתב ב-6.9 ולא רץ. הוא מתקן חמישה דברים, ושניים מהם נוגעים
ישירות לצ'ק-אין:

- **אפשר היה למחוק דגל אדום בלי שתדע.** ילד שסימן «חולה» או «כואב,
  מפריע לשחק» יכול היה למחוק את הדיווח, ואצלך היום היה נראה נקי. מהיום
  המחיקה הישירה חסומה, והזכות של הילד למחוק את התשובות שלו משאירה
  **שורה אחת ביומן** (כמה נמחקו ובין אילו תאריכים, בלי התשובות עצמן).
- **«נשמר 90 יום» לא היה מחובר לכלום.** מהיום הצ'ק-אין נכלל במנגנון
  הניקוי. ⚠ הוא עדיין **סופר ולא מוחק** (כך כל המנגנון, במכוון).
  המחיקה בפועל היא שורה ידנית פעם בחודש — ראו למטה.

ההסבר המלא על שלושת התיקונים האחרים נמצא ב-`הרצת_SQL_6.9.md`.

**שאילתת בדיקה (אחרי שלב 2):**

```sql
select count(*) as should_be_3
  from pg_policies
 where tablename = 'player_checkins'
   and policyname in ('pc_player_read', 'pc_player_insert', 'pc_player_update');
```

צריך לקבל `3`. אם קיבלת `0` — הקובץ לא רץ.

**ביטול:** בסוף `supabase_pilot_fixes_6_9.sql` יש בלוק ביטול מלא.

---

## שלב 3 — הדגל שנסגר חוזר (`supabase_checkin_flags_12_9.sql`)

**שני באגים שהתגלו עכשיו, שניהם במסד:**

**1. דגל אדום שסגרת בבוקר בלע את הדיווח של אחר הצהריים.**
לכל ילד יש שורה אחת ליום, והוא מעדכן אותה כל היום. ברגע שהקשת «דיברתי
איתו», הדגל נסגר — ולא נפתח שוב, גם אם הילד שינה את התשובה. התרחיש
שקרה בפועל: הילד סימן «אני חולה» → הקשת «דיברתי איתו» → הילד לחץ «בעצם
אני בסדר» → בצהריים סימן «כואב · מפריע לשחק» — ואצלך **לא הופיע כלום**.
מהיום, ברגע שהילד משנה תשובה, «ראיתי» ו«דיברתי איתו» נפתחים מחדש והשורה
חוזרת לרשימה שלך. אם הוא הקיש שוב על אותה תשובה בדיוק — כלום לא משתנה.

**2. «ראיתי את כולם» סימן גם שורות ריקות.**
ילד שביטל «אני חולה» משאיר שורה בלי אף תשובה. המסך שלך לא סופר אותה
כדיווח (וזה נכון), אבל השרת כן סימן אותה — ולכן קיבלת «הכול כבר היה
מסומן, לא היה מה לסמן» בדיוק כשמשהו כן סומן. מהיום שניהם מסכימים.

**3.** ובנוסף, שורה אחת שסוגרת את פרצת המחיקה גם אם משום מה לא הרצת את
שלב 2 (השורות לא מתנגשות — אפשר להריץ את שניהם בכל סדר).

**שאילתת בדיקה (אחרי שלב 3):**

```sql
select
  (select prosrc like '%handled_at%'
     from pg_proc where proname = 'checkins_touch'
      and pronamespace = 'public'::regnamespace)      as flag_reopens,
  (select prosrc like '%energy is not null%'
     from pg_proc where proname = 'ack_checkins'
      and pronamespace = 'public'::regnamespace)      as ack_skips_empty,
  has_table_privilege('authenticated', 'public.player_checkins', 'delete')
                                                      as delete_must_be_false;
```

צריך לקבל בדיוק: `true`, `true`, `false`.

**בדיקה חיה (לא חובה, שתי דקות):** בקש מילד לסמן «אני חולה», הקש אצלך
«דיברתי איתו», ואז בקש ממנו ללחוץ «בעצם אני בסדר» ולסמן «כואב · מפריע
לשחק». השורה צריכה לחזור אליך ל«דברים לביצוע».

**ביטול:** בסוף `supabase_checkin_flags_12_9.sql` יש בלוק ביטול,
סעיף-סעיף. הביטול המהיר של הכול (מחזיר את שני הבאגים):

```sql
create or replace function public.checkins_touch()
returns trigger language plpgsql as $$
begin new.updated_at := now(); return new; end $$;
notify pgrst, 'reload schema';
```

---

## פעם בחודש — המחיקה בפועל של דיווחים ישנים

זה **לא רץ לבד**. אחרי שלב 2, כדי למחוק בפועל את הדיווחים שעברו 90 יום:

```sql
select public.checkins_purge(false);
```

רוצה קודם רק לראות כמה יימחקו, בלי למחוק?

```sql
select public.checkins_purge(true);
```

---

## מה עוד פתוח (לא בקבצים האלה — צריך החלטה שלך)

- **ילד שרשום אצלך בשתי קבוצות מדווח רק לאחת.** המסד מחזיק דיווח אחד
  לילד ליום בלי קשר לקבוצה, והכרטיס נכתב תמיד על הקבוצה הראשונה שלו.
  הקבוצה השנייה תראה אותו לנצח כ«לא דיווח». צריך להחליט: כרטיס אחד לכל
  קבוצה, או בורר קבוצה בכרטיס.
- **«דיווחו 3 מתוך 3» כשבקבוצה 14 ילדים.** הרצועה סופרת רק ילדים עם
  חשבון. אין היום שום דרך שתרשום דיווח **בשביל** ילד בלי חשבון, אף
  שהמסד בנוי לזה. צריך להחליט אם מוסיפים כפתור «רשום עבורו».
