# הרצת SQL — 4.9.2026

קובץ אחד. שתי דקות. בלעדיו האפליקציה ממשיכה לעבוד — פשוט בלי הצ'ק-אין
(הכרטיס אצל השחקן והרצועה אצלך לא מוצגים, בלי שגיאות).

---

## מה זה נותן

**צ'ק-אין בוקר לשחקנים (פיילוט נערים א׳):** כל בוקר, מ-06:00, השחקן רואה
בבית שלו כרטיס עם שלוש שאלות — כמה ישן, איך האנרגיה, איך הגוף. הכול
בכפתורים, בלי טקסט חופשי. «כואב» פותח בחירת אזור + «מפריע לשחק?». יש גם
«אני חולה היום» ו«לא היום» (מסתיר את הכרטיס להיום, לא נשמר כלום).

**מה אתה רואה:** בבית, על כרטיס האימון הקרוב, רצועת «מוכנות היום» —
«דיווחו 8 מתוך 12 · 2 לשים לב: יובל, איתי». לחיצה פותחת את כל הרשימה:
אדום (כאב שמפריע / חולה), צהוב (פחות מ-7 שעות שינה), תקין, ומי שלא דיווח.
כאב שמפריע או מחלה יוצרים גם שורה ב«דברים לביצוע» — לחיצה עליה מסמנת
«דיברתי איתו» וסוגרת אותה. בלי שום הודעה אוטומטית להורים.

**עוד שניים קטנים באותו קובץ:**
- «בלי שאלות בוקר» — כפתור כיבוי לילד בודד (בפרטי השחקן בסגל), להורה שביקש.
- «עוזר מאמן» — סימון התפקיד שלך בקבוצה (בטאב הסגל), מוצג כתג. תצוגה בלבד.

**פרטיות:** התשובות נראות רק לך ולשחקן עצמו. לא בתיק השחקן, לא בדוח
המודפס, לא בכרטיס השחקן. נמחקות אחרי 90 יום (ראו «ניקוי» למטה).
סימוני «ראיתי»/«דיברתי איתו» שלך מוגנים גם ברמת המסד — שחקן לא יכול
לסמן אותם בעצמו, אפילו בקריאת API ידנית.

**⚠ פער ידוע — «הנתונים שלי» (4.9):** ייצוא הנתונים העצמי (`my_data_export`
בקובץ `supabase_my_data.sql`) עוד **לא** כולל את תשובות הצ'ק-אין. הקובץ
ההוא כבר רץ בייצור ואסור לערוך קובץ שרץ — נדרש קובץ המשך קטן שמוסיף מדור
'checkins' לייצוא. מחיקה כן מכוסה כבר עכשיו (מחיקת חשבון מוחקת את
הדיווחים אוטומטית, ויש `delete_my_checkins()`), אבל מי שמייצא «את כל
המידע עלי» לא יראה שם את תשובות הבוקר. **לסגור לפני שהפיילוט מתרחב מעבר
לקבוצה שלך.**

---

## הקובץ: `supabase_checkins_4_9.sql`

### סדר

**אחרי #44** (`supabase_coach_only_22_8.sql`) **ו-#45**
(`supabase_roster_link_merge_3_9.sql`). כל שאר התלויות (הרשאות, שער
ההסכמה, טבלת השמירה) כבר רצו מזמן — הקובץ בודק לבד ומדלג בשקט אם משהו חסר.

### איך מריצים

Supabase → SQL Editor → New query → מדביקים את כל תוכן הקובץ → Run.
בטוח להרצה חוזרת.

### לבדוק שעבד

```sql
-- 1) הטבלה קיימת (שורה אחת)
select to_regclass('public.player_checkins');

-- 2) שלוש המדיניויות + שער ההסכמה (4 שורות)
select policyname from pg_policies where tablename = 'player_checkins';

-- 3) העמודות החדשות (שתי שורות)
select table_name, column_name from information_schema.columns
 where (table_name = 'team_players' and column_name = 'wellness_off')
    or (table_name = 'team_iba' and column_name = 'coach_role');

-- 4) שורת השמירה — 90 יום (שורה אחת)
select key, days from public.retention_policy where key = 'player_checkins';
```

ואז באפליקציה: להיכנס עם חשבון שחקן מחובר — הכרטיס «הצ׳ק-אין של הבוקר»
מופיע ראשון בבית (אחרי 06:00). לענות, ואצלך בבית תופיע רצועת «מוכנות היום».

### ניקוי (פעם בחודש, ידני)

המחיקה של 90 הימים **לא רצה לבד** — כמו כל שאר חלונות השמירה, היא מחכה
לאישור עורך הדין. פעם בחודש, ב-SQL Editor:

```sql
select public.checkins_purge();       -- הרצה יבשה: כמה שורות היו נמחקות
select public.checkins_purge(false);  -- מחיקה בפועל (רק אחרי שהיבש נראה הגיוני)
```

המספר 90 יושב בטבלת `retention_policy` — שינוי שלו הוא שורת update שם,
בלי מיגרציה חדשה.

### ביטול

לכבות את הצ'ק-אין בלי למחוק כלום (השחקנים מפסיקים לכתוב, מה שנאסף נשאר לך):

```sql
drop policy if exists pc_player_own on public.player_checkins;
notify pgrst, 'reload schema';
```

למחוק הכול (בלוק «ביטול» המלא נמצא בסוף קובץ ה-SQL עצמו):

```sql
drop table if exists public.player_checkins cascade;
alter table public.team_players drop column if exists wellness_off;
alter table public.team_iba drop column if exists coach_role;
notify pgrst, 'reload schema';
```
