# הרצת SQL — 3.9.2026

קובץ אחד. שתי דקות. אפשר גם לא להריץ עכשיו — האפליקציה עובדת בלעדיו.

---

## מה השתנה בקוד (בלי SQL)

מ-3.9 המאמן **תמיד** רושם על שורת הסגל — עומס 1–10, «עמד ביעד», יעדים, משוב,
משימות ו«ביצע» — גם עכשיו כשצד השחקן פתוח לפיילוט. מה שהשחקן רושם בעצמו
(דירוג עצמי, «ביצעתי») מופיע **ליד** מה שרשמת, לא במקומו. ההערה בסקירת
האימון היא פרטית שלך («רק אתה רואה») — לא נשלחת לשחקן.

כשילד מתחבר בקוד ההצטרפות, האפליקציה מחפשת בסגל שורה עם אותו שם שעוד לא
מחוברת לחשבון — ומחברת אותה (במקום לפתוח שורה כפולה). אם יש כמה או אף אחת —
נפתחת שורה חדשה ואתה מקבל רמז לחבר ידנית.

---

## הקובץ: `supabase_roster_link_merge_3_9.sql`

**למה הוא צריך להיות:** אחרי החיבור, היעדים והמשימות שכבר רשמת לילד יושבים
על השורה שלו בסגל — אבל הוא, מהצד שלו, רואה רק מה שכתוב על **החשבון** שלו.
הקובץ מוסיף כלל קטן במסד: ברגע ששורה בסגל מקבלת חשבון, כל יעד וכל משימה
אישית שיושבים עליה מקבלים גם את החשבון. אם תנתק את החשבון — זה חוזר לאחור.

**מה הוא עושה:**
1. פונקציה + טריגר על טבלת הסגל (`team_players`) — רץ אוטומטית בכל חיבור/ניתוק
   (כולל החלפת חשבון: השורות עוברות מהחשבון הישן לחדש).
2. השלמה חד-פעמית לשורות שכבר מחוברות היום.
3. תיקון פרטיות חד-פעמי: הערות הסקירה שנכתבו בין 22.8 ל-3.9 לשחקנים מחוברים
   נשמרו בטעות גם על החשבון של השחקן — כלומר מהיום, כשצד השחקן פתוח, הוא
   היה יכול לקרוא אותן. הבלוק מנתק אותן מהחשבון והן חוזרות להיות «רק אתה
   רואה». משוב שנשלח בכוונה («שליחת משוב לשחקן») לא נפגע.
   (אם תעדיף שלא לגעת בנתונים — אפשר למחוק את סעיף 4 מהקובץ לפני ההרצה,
   אבל דע שהערות מהתקופה הזו יישארו גלויות לשחקנים המחוברים.)

**מה הוא לא עושה:** לא נוגע בעומס (`session_effort`) ולא ב«עמד ביעד»
(`session_goal_marks`). לא מוחק כלום.

### סדר

**אחרי #44** (`supabase_coach_only_22_8.sql`). אם #44 לא רץ — הקובץ מדלג
בשקט על ההשלמה ומדפיס הודעה.

### איך מריצים

Supabase → SQL Editor → New query → מדביקים את כל תוכן הקובץ → Run.
בטוח להרצה חוזרת.

### לבדוק שעבד

```sql
-- 1) הטריגר קיים (שורה אחת)
select tgname from pg_trigger where tgname = 'roster_link_merge_trg';

-- 2) אין יותר יעדים «יתומים» על שורה שכבר מחוברת (צריך להחזיר 0)
select count(*) from public.player_goals g
  join public.team_players tp on tp.id = g.roster_id
 where g.player_id is null and tp.player_id is not null;

-- 3) אין יותר משימות אישיות «יתומות» על שורה שכבר מחוברת (צריך להחזיר 0)
select count(*) from public.player_assignments a
  join public.team_players tp on tp.id = a.roster_id
 where a.player_id is null and a.team is null and tp.player_id is not null;

-- 4) אין יותר הערות סקירה שגלויות לשחקן (צריך להחזיר 0)
select count(*) from public.player_feedback
 where session_id is not null and roster_id is not null and player_id is not null;
```

### ביטול

```sql
drop trigger if exists roster_link_merge_trg on public.team_players;
drop function if exists public.roster_link_merge();
notify pgrst, 'reload schema';
```

מה שכבר מוזג נשאר ממוזג — וזה בסדר: יעד עם שני המזהים נקרא גם אצלך
(לפי שורת הסגל) וגם אצל השחקן (לפי החשבון). שום נתון לא נמחק.
