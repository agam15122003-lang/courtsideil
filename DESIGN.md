# DESIGN.md — השפה העיצובית של CourtSide: "פרקט ולילה"

> המסמך האופרטיבי לכל עבודת UI בפרויקט. הרקע המחקרי ב-[DESIGN_RESEARCH.md](DESIGN_RESEARCH.md).
> עיקרון-על: **בהירות, עקביות, ריסון** — נייבי-לילה של ערב משחק, כתום לוהט של כדור, וטיפוגרפיה שעושה את העבודה.

## 0. שני עולמות, משפחה אחת

| | עולם המאמן | עולם השחקן (ילדים/נוער) |
|---|---|---|
| אופי | **רציני-פרימיום** — כלי עבודה מקצועי | **שובב-אנרגטי** — צבע, קונפטי, עידוד |
| כותרות | Heebo 800-900, tracking שלילי | Karantina ענק + אימוג'י בטקסט |
| צבע | מקסימום 2 מבטאים למסך | פסי גרדיאנט קטגוריאליים, פסי התקדמות |
| מוטיבציה | נתונים, שליטה, חיסכון בזמן | רצף, התקדמות, "המאמן ראה" |

שניהם על אותם טוקנים — ההבדל הוא במינון, לא בפלטה.

## 1. פלטת צבעים

**חוק ברזל: אין hex גולמי ב-JSX/CSS חדש — טוקנים בלבד** (חריג: קישוט טהור כמו קונפטי).

### לילה (רקעים כהים, hero, sidebar)
| טוקן | בהיר | שימוש |
|---|---|---|
| `--navy-900` / `--hero-navy` | `#0A1020` | hero, פסי לילה, sidebar |
| `--navy-800` | `#101A33` | נקודת ביניים בגרדיאנט hero |
| `--hero-navy-2` | `#16233F` | קצה הגרדיאנט |

### כתום לוהט (המבטא)
| טוקן | ערך | שימוש | ניגודיות |
|---|---|---|---|
| `--accent` | `#F97316` | מסגרות, tints, גרפיקה גדולה | **לא לטקסט על בהיר!** |
| `--accent-fill` | `#C2410C` | מילוי כפתורים (טקסט לבן) | 5.5:1 ✓ |
| `--accent-strong` | `#9A3412` | טקסט/אייקון כתום על בהיר | 7:1+ ✓ |
| `--accent-soft` | `rgba(249,115,22,.12)` | רקעי tint |
| `--orange-300` | `#FDBA74` | מבטא על רקע לילה |

### תפקידים נוספים
| טוקן | ערך | שימוש |
|---|---|---|
| `--bg` | `#F6F7FB` | קנבס בהיר |
| `--surface` / `--surface-alt` | `#FFFFFF` / `#EDF2F8` | כרטיסים / בועות |
| `--c-green` | `#15803D` | הצלחה, פסי התקדמות (גרדיאנט → `#4ADE80`) |
| `--gold-wood` | `#C8873F` | **נגיעות בלבד**: MVP, מאמן השבוע, הישגים |
| `--c-blue/purple/red` | (קיימים) | קטגוריות; סגול = hero משחק + מטרות לשיפור |

מצב כהה: אותם תפקידים, ערכים ב-`[data-theme='dark']` (כתום מואר `#FB923C/#FDBA74` כטקסט). **כל צבע חדש חייב override לדארק אם הוא קשיח.**

## 2. טיפוגרפיה עברית

| טוקן | פונט | תפקיד | איפה |
|---|---|---|---|
| `--font` | **Rubik** 400-800 | גוף, UI | הכל |
| `--font-display` | **Heebo** 700-900 | כותרות מסך/סקשן | H1/H2, כותרות כרטיסים |
| `--font-sport` | **Karantina** 300/400/700 | **כותרות-ענק ומספרים בלבד** | hero נחיתה, שם שחקן ב-hero, טיימרים, מספרי סטטיסטיקה |

חוקים:
- Karantina **לעולם לא** לטקסט גוף, לא מתחת ל-22px, ולא לפסקאות — הוא condensed וקשה לקריאה קטן.
- מספרים (טיימר, סטטיסטיקה, X/Y): `font-variant-numeric: tabular-nums` תמיד.
- גוף: 16px בסיס, line-height 1.5. **מינימום מוחלט 12px** (יש רצפה גלובלית ב-CSS).
- כותרת display **אחת** לכל מסך (H1). eyebrow קטן מעליה כשצריך הקשר.
- הפונטים נטענים ב-`index.html` (Google Fonts link אחד) — לא להוסיף `@import`.

## 3. אנימציה — עדין, קצר, משמעותי

- **משך: 150-250ms** למעברי UI (hover, פתיחת כרטיס). כניסות מסך עד 380ms (`--dur-slow`). פסי התקדמות עד 600ms.
- **Easing**: `var(--ease-out)` כברירת מחדל; `var(--ease-spring)` רק לרגעי חגיגה (פס מתמלא).
- **מה מותר**: fade+rise בכניסה (`.pl-stagger`), מילוי פסים, scroll-reveal בנחיתה, קונפטי בהשלמה (`burstConfetti()` מ-`src/confetti.js`).
- **מה אסור**: סיבובים/קפיצות דקורטיביים, אנימציה של width/height בלייאאוט, parallax כבד, אפקט "בשביל האפקט".
- **חוזה נגישות (חובה על כל אנימציה חדשה)**: כיבוי גם ב-`@media (prefers-reduced-motion: reduce)` וגם ב-`html.a11y-motion` (ווידג'ט הנגישות). קונפטי מדלג לגמרי.
- **Framer Motion**: קיים בענף `design-overhaul` (טרם מוזג) עם `LazyMotion` + `motionOff()`. כשמשתמשים בו — מודלים/גיליונות בלבד, לא כניסות מסך (לקח מתועד: `AnimatePresence mode="wait"` על החלפת views נתקע בטאבים ברקע). ב-main: CSS-first.

## 4. חוקי RTL

- `<html lang="he" dir="rtl">` — הכיוון הראשי תמיד RTL; אנגלית דרך `L('עברית','English')`.
- **מספרים, שעונים, תאריכים מספריים, X/Y**: תמיד `dir="ltr"` על האלמנט (או `<bdi>`) — אחרת "100/200" מתהפך.
- **חץ "קדימה" בעברית הוא ←** (ArrowLeft). "לכל המטרות ←". לעולם לא →.
- מרווחים לוגיים: `margin-inline-start/end`, `inset-inline` — לא left/right פיזיים (חוץ ממיקום אבסולוטי דו-כיווני מכוון).
- אייקון לפני טקסט = מימין בעברית; flex עם gap עושה את זה לבד — לא להוסיף margin ידני.
- קלט טלפון/אימייל/קוד: `dir="ltr"` עם `text-align: start`.
- placeholder עברי בשדה password — בלי `dir="ltr"` על השדה (באג מתועד).

## 5. עשה / אל-תעשה

```jsx
// ✅ עשה: טוקן + חץ RTL + מספר מוגן
<button className="plhg-all" onClick={goAll}>
  {L('לכל המטרות', 'All goals')} <ArrowLeft size={14} />
</button>
<b dir="ltr">{prog}/{target}</b>

// ❌ אל תעשה: hex גולמי, חץ הפוך, מספר מתהפך
<button style={{ color: '#F97316' }}>לכל המטרות ←</button>   // hex + חץ בתוך המחרוזת
<b>{prog}/{target}</b>                                        // יתהפך ב-RTL
```

```css
/* ✅ עשה: גרדיאנט-מבטא אחד, עם תפקיד */
.land-band { background: linear-gradient(150deg, var(--hero-navy), var(--hero-navy-2)); }

/* ❌ אל תעשה: גרדיאנט גנרי סגול-ורוד "סטארטאפי" על כרטיס רגיל */
.card { background: linear-gradient(135deg, #667eea, #764ba2); }
```

```jsx
// ✅ עשה: אייקון lucide עם תווית
<button aria-label={L('סגור', 'Close')}><X size={18} /></button>

// ❌ אל תעשה: אימוג'י כאייקון UI (אימוג'י מותר רק בתוך מחרוזות תוכן)
<button>❌</button>
```

## 6. שלושת המצבים — חובה לכל מסך

כל מסך/רכיב ששולף נתונים חייב שלושה מצבים מעוצבים:

1. **טעינה**: `<div className="app-loading"><div className="loader" /></div>` (או skeleton). לעולם לא מסך לבן.
2. **ריק**: `.empty-state` עם אייקון (`.empty-ic`), כותרת (`.empty-title`), ושורת "מה עושים עכשיו" — טון חם, לא "אין נתונים".
3. **שגיאה**: הודעה + דרך התאוששות (`toast.error` לפעולות; לרכיב שנכשל — טקסט שגיאה, לא ריק מזויף). שאילתא שנכשלת ≠ "אין נתונים" — לא להציג empty-state על error.

דפוסים קיימים לחיקוי: `MyAssignments` (שלושתם), `HomeGoals` (ריק עם CTA), `Notifications` (נעלם בשקט אם הטבלה חסרה — מותר רק לרכיבי צד).

## 7. רכיבים — כללי אצבע

- כרטיס ראשי: `border-radius: 18-20px`, `border: 1px solid var(--border)`, `box-shadow: var(--shadow-sm)`; hover: הרמה עדינה + `--shadow-md`.
- CTA ראשי **אחד** למסך (חוק Hick); משני = `.btn-soft`/ghost.
- יעדי מגע ≥44px תחת `pointer: coarse` (יש בלוק גלובלי — כפתור חדש קטן מזה צריך override שם).
- CSS חדש = בלוק append **בסוף** `index.css` עם banner מתוארך. לא עורכים בלוקים ישנים (הכללים המאוחרים מנצחים; שים לב לספציפיות של סלקטורים מקוננים קיימים כמו `.home-hero .home-greet-title`).
